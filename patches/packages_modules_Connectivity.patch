diff --git a/Tethering/src/com/android/networkstack/tethering/Tethering.java b/Tethering/src/com/android/networkstack/tethering/Tethering.java
index c44a0def8f..a26d51d136 100644
--- a/Tethering/src/com/android/networkstack/tethering/Tethering.java
+++ b/Tethering/src/com/android/networkstack/tethering/Tethering.java
@@ -993,14 +993,52 @@ public class Tethering {
         }
     }
 
+    // device/vivo/1907N: when the AP interface is a freshly, dynamically created one (this
+    // device's dual-interface concurrent STA+AP wifi mode creates a brand new "wlanN" netdev
+    // per SoftAp session, instead of reusing the always-present "wlan0"), this device's
+    // Tethering process never receives netd's own interfaceAdded notification for it at all -
+    // confirmed via logcat across many attempts: "AddSingleAccessPoint, iface=wlan2"
+    // immediately followed by "Tried to Tether an unknown iface: wlan2", with no amount of
+    // waiting (tried up to several seconds) ever making mTetherStates learn about it.
+    // Meanwhile netd itself is fully aware of the interface the whole time (confirmed via
+    // "netd: interfaceGetCfg(wlan2)" succeeding on every attempt) - only the push
+    // notification to this process is missing, not netd's own knowledge of the interface.
+    // Rather than wait for a notification that may never arrive, register the interface
+    // directly here (mirroring exactly what interfaceAdded()/processInterfaceStateChange()
+    // would have done) whenever WifiService asks to tether an iface we don't know about yet.
+    // The newly created IpServer still needs a moment to run its own state machine up to
+    // STATE_AVAILABLE before tether() will accept it, so poll briefly for that (this wait is
+    // local/in-process, unlike the netd notification, so it's expected to resolve quickly).
+    private static final int TETHER_IFACE_AVAILABLE_MAX_RETRIES = 20;
+    private static final int TETHER_IFACE_AVAILABLE_RETRY_DELAY_MS = 50;
+
     void tether(String iface, int requestedState, final IIntResultListener listener) {
         mHandler.post(() -> {
-            try {
-                listener.onResult(tether(iface, requestedState));
-            } catch (RemoteException e) { }
+            if (!mTetherStates.containsKey(iface)) {
+                Log.w(TAG, "tether(" + iface + "): iface not yet known, registering it "
+                        + "directly instead of waiting for a netd notification that may "
+                        + "never arrive");
+                ensureIpServerStarted(iface);
+            }
+            tetherWhenAvailable(iface, requestedState, listener, 0 /* retryCount */);
         });
     }
 
+    private void tetherWhenAvailable(String iface, int requestedState,
+            final IIntResultListener listener, int retryCount) {
+        final TetherState tetherState = mTetherStates.get(iface);
+        if (tetherState != null && tetherState.lastState != IpServer.STATE_AVAILABLE
+                && retryCount < TETHER_IFACE_AVAILABLE_MAX_RETRIES) {
+            mHandler.postDelayed(
+                    () -> tetherWhenAvailable(iface, requestedState, listener, retryCount + 1),
+                    TETHER_IFACE_AVAILABLE_RETRY_DELAY_MS);
+            return;
+        }
+        try {
+            listener.onResult(tether(iface, requestedState));
+        } catch (RemoteException e) { }
+    }
+
     private int tether(String iface, int requestedState) {
         if (DBG) Log.d(TAG, "Tethering " + iface);
         TetherState tetherState = mTetherStates.get(iface);
diff --git a/netbpfload/NetBpfLoad.cpp b/netbpfload/NetBpfLoad.cpp
index 8af4dd49a7..44a3e6181a 100644
--- a/netbpfload/NetBpfLoad.cpp
+++ b/netbpfload/NetBpfLoad.cpp
@@ -113,7 +113,26 @@ int loadAllElfObjects(const android::bpf::Location& location) {
             bool critical;
             int ret = android::bpf::loadProg(progPath.c_str(), &critical, location);
             if (ret) {
-                if (critical) retVal = ret;
+                // device/vivo/1907N: kernel 4.14.336 (kept deliberately - see device git
+                // history) predates BTF support entirely (no CONFIG_DEBUG_INFO_BTF Kconfig
+                // option exists in this kernel's Kconfig tree at all). Originally only
+                // @btf.o-suffixed (CO-RE relocated) objects were excepted here, but a second
+                // real boot log showed netd_shared/netd.o (no @btf suffix at all) hitting the
+                // exact same bpf_create_map ret=-22 for its own configuration_map - modern
+                // libbpf-style BPF maps carry BTF key/value type annotations by default
+                // regardless of whether the containing object uses CO-RE relocations, so this
+                // isn't limited to @btf.o files. Matching on EINVAL specifically (rather than
+                // the filename) catches this whole class of "map/program needs kernel BTF
+                // metadata that doesn't exist" failure while still treating any OTHER errno
+                // (ENOMEM, EPERM, etc. - a real bug, not a known kernel limitation) as fatal.
+                // Previously this made the whole netbpfload process exit(2) before ever
+                // exec'ing into /system/bin/bpfloader, which is the only thing that sets
+                // bpf.progs_loaded - starving netd of that property forever, which made
+                // system_server's Watchdog kill the entire zygote every ~90s in an infinite
+                // loop (confirmed via a real boot log: Watchdog blocked in
+                // NetworkManagementService.connectNativeNetdService for 66s, then "KILLING
+                // SYSTEM PROCESS").
+                if (critical && ret != -EINVAL) retVal = ret;
                 ALOGE("Failed to load object: %s, ret: %s", progPath.c_str(), std::strerror(-ret));
             } else {
                 ALOGI("Loaded object: %s", progPath.c_str());
diff --git a/netbpfload/netbpfload.mainline.rc b/netbpfload/netbpfload.mainline.rc
index 0ac5de8b97..44cb2dc901 100644
--- a/netbpfload/netbpfload.mainline.rc
+++ b/netbpfload/netbpfload.mainline.rc
@@ -4,5 +4,12 @@ service bpfloader /apex/com.android.tethering/bin/netbpfload
     user root
     rlimit memlock 1073741824 1073741824
     oneshot
-    reboot_on_failure reboot,bpfloader-failed
+    # device/vivo/1907N: this is the APEX-bundled ("mainline") copy of the bpfloader service
+    # declaration, packaged into com.android.tethering as e.g. netbpfload.33rc - it carries
+    # `override` and gets parsed ~10s after the plain /system/etc/init/netbpfload.rc copy, so
+    # it wins and silently reintroduced reboot_on_failure even after that other file's copy of
+    # the same line was disabled (see netbpfload.rc's own comment / device git history for the
+    # full reasoning: kernel 4.14.336 has no BTF support, offload@btf.o's CO-RE map creation
+    # structurally cannot succeed here).
+    #reboot_on_failure reboot,bpfloader-failed
     override
diff --git a/netbpfload/netbpfload.rc b/netbpfload/netbpfload.rc
index 14181dc6fc..8d96f5e28d 100644
--- a/netbpfload/netbpfload.rc
+++ b/netbpfload/netbpfload.rc
@@ -81,6 +81,17 @@ service bpfloader /system/bin/netbpfload
     #    'cannot prove return value is 0 or 1' or 'unsupported / unknown operation / helper',
     #    'invalid bpf_context access', etc.
     #
-    reboot_on_failure reboot,bpfloader-failed
+    # device/vivo/1907N: kernel 4.14.336 (kept deliberately - see device git history) predates
+    # BTF support entirely (no CONFIG_DEBUG_INFO_BTF Kconfig option exists in this kernel's
+    # Kconfig tree at all - BTF landed in mainline Linux 4.18+). offload@btf.o's CO-RE map
+    # creation ("tether_downstream6_map") hard-fails with EINVAL on this kernel
+    # (bpf_create_map ret=-22) - a structural kernel-version limit, not something fixable via
+    # defconfig. The plain (non-BTF) test.o/offload.o objects DO load fine, but netbpfload
+    # treats them as legacy/superseded no-ops once a newer bpfloader (ours: 0x29) is present,
+    # so only the @btf variant actually provides the real tethering-offload maps. Per this
+    # file's own documented debug procedure (see the numbered comment above,
+    # step 2) - disabling this turns a full device reboot loop into, at worst, a narrower
+    # tethering-hardware-offload feature loss rather than an unbootable device.
+    #reboot_on_failure reboot,bpfloader-failed
     # we're not really updatable, but want to be able to load bpf programs shipped in apexes
     updatable
diff --git a/netd/BpfHandler.cpp b/netd/BpfHandler.cpp
index 2252e07fc9..75bf1b304e 100644
--- a/netd/BpfHandler.cpp
+++ b/netd/BpfHandler.cpp
@@ -111,12 +111,33 @@ static Status initPrograms(const char* cg2_path) {
         ALOGE("Failed to open the cgroup directory: %s", strerror(err));
         return statusFromErrno(err, "Open the cgroup directory failed");
     }
-    RETURN_IF_NOT_OK(checkProgramAccessible(XT_BPF_ALLOWLIST_PROG_PATH));
-    RETURN_IF_NOT_OK(checkProgramAccessible(XT_BPF_DENYLIST_PROG_PATH));
-    RETURN_IF_NOT_OK(checkProgramAccessible(XT_BPF_EGRESS_PROG_PATH));
-    RETURN_IF_NOT_OK(checkProgramAccessible(XT_BPF_INGRESS_PROG_PATH));
-    RETURN_IF_NOT_OK(attachProgramToCgroup(BPF_EGRESS_PROG_PATH, cg_fd, BPF_CGROUP_INET_EGRESS));
-    RETURN_IF_NOT_OK(attachProgramToCgroup(BPF_INGRESS_PROG_PATH, cg_fd, BPF_CGROUP_INET_INGRESS));
+    // device/vivo/1907N: kernel 4.14.336 has zero BTF support, so every one of the xt_bpf/
+    // cgroup-bpf programs below (built with modern libbpf/BTF-annotated maps) fails to load -
+    // confirmed via a real boot: netbpfload already tolerates the underlying bpf_create_map
+    // EINVAL (patches/packages_modules_Connectivity.patch, NetBpfLoad.cpp), but netd itself
+    // used to treat ANY of these programs being unpinned as fatal (RETURN_IF_NOT_OK bubbling up
+    // to NetdUpdatable.cpp's libnetd_updatable_init(), which called abort() unconditionally),
+    // crash-looping netd forever and blocking the whole boot. None of this can ever work on
+    // this kernel, so it's made best-effort here instead: log and move on rather than fail
+    // initPrograms()/abort() when a program can't be found or attached. This does mean the
+    // xt_bpf-based per-UID firewall allow/deny lists and cgroup traffic accounting are
+    // unavailable on this device - acceptable, since the alternative is netd never starting
+    // at all.
+    for (const auto& progPath : {XT_BPF_ALLOWLIST_PROG_PATH, XT_BPF_DENYLIST_PROG_PATH,
+                                  XT_BPF_EGRESS_PROG_PATH, XT_BPF_INGRESS_PROG_PATH}) {
+        Status res = checkProgramAccessible(progPath);
+        if (!netdutils::isOk(res)) {
+            ALOGW("BTF-less kernel: %s (continuing without it)", toString(res).c_str());
+        }
+    }
+    for (const auto& [progPath, cgType] :
+         {std::pair{BPF_EGRESS_PROG_PATH, BPF_CGROUP_INET_EGRESS},
+          std::pair{BPF_INGRESS_PROG_PATH, BPF_CGROUP_INET_INGRESS}}) {
+        Status res = attachProgramToCgroup(progPath, cg_fd, cgType);
+        if (!netdutils::isOk(res)) {
+            ALOGW("BTF-less kernel: %s (continuing without it)", toString(res).c_str());
+        }
+    }
 
     // For the devices that support cgroup socket filter, the socket filter
     // should be loaded successfully by bpfloader. So we attach the filter to
@@ -124,25 +145,29 @@ static Status initPrograms(const char* cg2_path) {
     // TODO: delete the if statement once all devices should support cgroup
     // socket filter (ie. the minimum kernel version required is 4.14).
     if (bpf::isAtLeastKernelVersion(4, 14, 0)) {
-        RETURN_IF_NOT_OK(
-                attachProgramToCgroup(CGROUP_SOCKET_PROG_PATH, cg_fd, BPF_CGROUP_INET_SOCK_CREATE));
+        Status res =
+                attachProgramToCgroup(CGROUP_SOCKET_PROG_PATH, cg_fd, BPF_CGROUP_INET_SOCK_CREATE);
+        if (!netdutils::isOk(res)) {
+            ALOGW("BTF-less kernel: %s (continuing without it)", toString(res).c_str());
+        }
     }
 
     if (bpf::isAtLeastKernelVersion(4, 19, 0)) {
-        RETURN_IF_NOT_OK(attachProgramToCgroup(
+        Status res4 = attachProgramToCgroup(
                 "/sys/fs/bpf/netd_readonly/prog_block_bind4_block_port",
-                cg_fd, BPF_CGROUP_INET4_BIND));
-        RETURN_IF_NOT_OK(attachProgramToCgroup(
+                cg_fd, BPF_CGROUP_INET4_BIND);
+        if (!netdutils::isOk(res4)) {
+            ALOGW("BTF-less kernel: %s (continuing without it)", toString(res4).c_str());
+        }
+        Status res6 = attachProgramToCgroup(
                 "/sys/fs/bpf/netd_readonly/prog_block_bind6_block_port",
-                cg_fd, BPF_CGROUP_INET6_BIND));
-
-        // This should trivially pass, since we just attached up above,
-        // but BPF_PROG_QUERY is only implemented on 4.19+ kernels.
-        if (bpf::queryProgram(cg_fd, BPF_CGROUP_INET_EGRESS) <= 0) abort();
-        if (bpf::queryProgram(cg_fd, BPF_CGROUP_INET_INGRESS) <= 0) abort();
-        if (bpf::queryProgram(cg_fd, BPF_CGROUP_INET_SOCK_CREATE) <= 0) abort();
-        if (bpf::queryProgram(cg_fd, BPF_CGROUP_INET4_BIND) <= 0) abort();
-        if (bpf::queryProgram(cg_fd, BPF_CGROUP_INET6_BIND) <= 0) abort();
+                cg_fd, BPF_CGROUP_INET6_BIND);
+        if (!netdutils::isOk(res6)) {
+            ALOGW("BTF-less kernel: %s (continuing without it)", toString(res6).c_str());
+        }
+        // Originally: "This should trivially pass... but BPF_PROG_QUERY is only implemented on
+        // 4.19+ kernels" followed by an unconditional abort() on failure - dropped, since on
+        // this device none of the above actually attached in the first place.
     }
 
     return netdutils::status::ok;
@@ -167,13 +192,28 @@ Status BpfHandler::init(const char* cg2_path) {
 }
 
 Status BpfHandler::initMaps() {
-    RETURN_IF_NOT_OK(mStatsMapA.init(STATS_MAP_A_PATH));
-    RETURN_IF_NOT_OK(mStatsMapB.init(STATS_MAP_B_PATH));
-    RETURN_IF_NOT_OK(mConfigurationMap.init(CONFIGURATION_MAP_PATH));
-    RETURN_IF_NOT_OK(mUidPermissionMap.init(UID_PERMISSION_MAP_PATH));
-    // initialized last so mCookieTagMap.isValid() implies everything else is valid too
-    RETURN_IF_NOT_OK(mCookieTagMap.init(COOKIE_TAG_MAP_PATH));
-    ALOGI("%s successfully", __func__);
+    // device/vivo/1907N: same BTF-less-kernel story as initPrograms() above - these pinned maps
+    // (built with modern BTF-annotated types) never get created by netbpfload on kernel
+    // 4.14.336, so .init() here always returns a graceful "Pinned map not accessible or does
+    // not exist" Result (BpfMap::init() already handles a missing fd cleanly, see BpfMap.h) -
+    // it's this function's own RETURN_IF_NOT_OK that turns that into a fatal error, which
+    // NetdUpdatable.cpp's libnetd_updatable_init() then turns into an abort(), crash-looping
+    // netd forever. The rest of BpfHandler already expects maps to potentially be invalid at
+    // the point of use (e.g. tagSocket()/hasUpdateDeviceStatsPermission() check isValid()
+    // first), so made this best-effort too: log and move on. Per-app data usage stats/tagging
+    // and the UID permission map are simply unavailable on this device as a result.
+    auto warnIfNotOk = [](const char* name, const Status& status) {
+        if (!netdutils::isOk(status)) {
+            ALOGW("BTF-less kernel: %s: %s (continuing without it)", name,
+                  toString(status).c_str());
+        }
+    };
+    warnIfNotOk("mStatsMapA", mStatsMapA.init(STATS_MAP_A_PATH));
+    warnIfNotOk("mStatsMapB", mStatsMapB.init(STATS_MAP_B_PATH));
+    warnIfNotOk("mConfigurationMap", mConfigurationMap.init(CONFIGURATION_MAP_PATH));
+    warnIfNotOk("mUidPermissionMap", mUidPermissionMap.init(UID_PERMISSION_MAP_PATH));
+    warnIfNotOk("mCookieTagMap", mCookieTagMap.init(COOKIE_TAG_MAP_PATH));
+    ALOGI("%s done (best-effort)", __func__);
 
     return netdutils::status::ok;
 }
diff --git a/service-t/src/com/android/server/NetworkStatsServiceInitializer.java b/service-t/src/com/android/server/NetworkStatsServiceInitializer.java
index 675e5a1057..c65cc4a11a 100644
--- a/service-t/src/com/android/server/NetworkStatsServiceInitializer.java
+++ b/service-t/src/com/android/server/NetworkStatsServiceInitializer.java
@@ -77,6 +77,23 @@ public final class NetworkStatsServiceInitializer extends SystemService {
     private NetworkStatsService maybeCreateNetworkStatsService(final Context context) {
         if (!SdkLevel.isAtLeastT()) return null;
 
-        return NetworkStatsService.create(context);
+        try {
+            return NetworkStatsService.create(context);
+        } catch (IllegalStateException e) {
+            // device/vivo/1907N: kernel 4.14.336 has no BTF support at all, so BpfNetMaps
+            // (backing NetworkStatsFactory's BPF-based per-UID accounting) can never open its
+            // pinned maps here - confirmed via a real boot: this used to be an uncaught
+            // "Cannot open netd configuration map" IllegalStateException from
+            // BpfNetMaps.getConfigurationMap(), which crashed the whole zygote/system_server
+            // (an uncaught exception from a system service constructor is fatal by AOSP
+            // design). The rest of this class already handles mStatsService == null
+            // gracefully (see onStart()/onBootPhase()), same as the SdkLevel check above -
+            // this just extends that same "optional service" handling to this failure mode.
+            // Per-app/per-UID network data usage stats are unavailable on this device as a
+            // result, consistent with the other BTF-related tradeoffs in this port (see
+            // device git history).
+            Log.e(TAG, "Failed to create NetworkStatsService, BPF maps unavailable", e);
+            return null;
+        }
     }
 }
diff --git a/service/jni/com_android_server_connectivity_ClatCoordinator.cpp b/service/jni/com_android_server_connectivity_ClatCoordinator.cpp
index c125bd6479..1496a157da 100644
--- a/service/jni/com_android_server_connectivity_ClatCoordinator.cpp
+++ b/service/jni/com_android_server_connectivity_ClatCoordinator.cpp
@@ -130,7 +130,17 @@ static void verifyClatPerms() {
 
 #undef V2
 
-    if (fatal) abort();
+    // device/vivo/1907N: kernel 4.14.336 has no BTF support at all, so the clatd BPF progs/maps
+    // checked above (built with modern BTF-annotated types) can never exist here - confirmed
+    // via a real boot backtrace through register_com_android_server_connectivity_ClatCoordinator.
+    // Upstream's own comment above says these failures are "99% likely to be build problems",
+    // true on Google's own hardware but not on this permanently BTF-less kernel, so the
+    // originally-unconditional abort() here just crash-looped system_server forever. Each
+    // failure is still logged via ALOGF/ALOGE above; only the process-ending abort() is
+    // dropped. 464xlat/CLAT (IPv6-only network IPv4 access) is unavailable on this device as a
+    // result, same tradeoff as the other BTF-related fixes in this port (see device git
+    // history).
+    if (fatal) ALOGE("verifyClatPerms failed (continuing without CLAT)");
 }
 
 #undef V
diff --git a/staticlibs/native/bpf_headers/include/bpf/BpfMap.h b/staticlibs/native/bpf_headers/include/bpf/BpfMap.h
index 1037bebf29..d646381c43 100644
--- a/staticlibs/native/bpf_headers/include/bpf/BpfMap.h
+++ b/staticlibs/native/bpf_headers/include/bpf/BpfMap.h
@@ -60,7 +60,16 @@ class BpfMapRO {
 
   protected:
     void abortOnMismatch(bool writable) const {
-        if (!mMapFd.ok()) abort();
+        // device/vivo/1907N: kernel 4.14.336 has no BTF support, so BPF maps built with modern
+        // BTF-annotated types (e.g. gpuservice's gpuwork map) never get pinned in the first
+        // place - !mMapFd.ok() is the normal, expected outcome here, not a code bug. Callers
+        // like frameworks/native/services/gpuservice/gpuwork/GpuWork.cpp's getBpfMap() are
+        // already written to handle this gracefully via isValid()/BpfMap's own constructor
+        // failing cleanly - the original unconditional abort() here defeated that entirely,
+        // hard-crashing gpuservice in an infinite loop. The other checks below (fd valid but
+        // flags/key/value size mismatched) are left as real abort()s - those genuinely can't
+        // happen without an actual code bug, per the class's own existing comments elsewhere.
+        if (!mMapFd.ok()) return;
         if (isAtLeastKernelVersion(4, 14, 0)) {
             int flags = bpfGetFdMapFlags(mMapFd);
             if (flags < 0) abort();
