diff --git a/src/java/com/android/internal/telephony/BaseCommands.java b/src/java/com/android/internal/telephony/BaseCommands.java
index 972884a77b..697d873d95 100644
--- a/src/java/com/android/internal/telephony/BaseCommands.java
+++ b/src/java/com/android/internal/telephony/BaseCommands.java
@@ -166,7 +166,10 @@ public abstract class BaseCommands implements CommandsInterface {
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     protected int mAllowedNetworkTypesBitmask;
     // CDMA subscription received from PhoneFactory
-    protected int mCdmaSubscription;
+    // vivo 1907N IMS bring-up: MtkRadioIndication (mediatek-telephony-common.jar, prebuilt)
+    // reads this compositionally through an "mRil"-typed field - it extends AOSP's own
+    // RadioIndication, not RIL/BaseCommands, so protected (subclass-only) isn't enough.
+    public int mCdmaSubscription;
     // Type of Phone, GSM or CDMA. Set by GsmCdmaPhone.
     @UnsupportedAppUsage
     protected int mPhoneType;
diff --git a/src/java/com/android/internal/telephony/PhoneFactory.java b/src/java/com/android/internal/telephony/PhoneFactory.java
index 3361b74710..22f15a7c61 100644
--- a/src/java/com/android/internal/telephony/PhoneFactory.java
+++ b/src/java/com/android/internal/telephony/PhoneFactory.java
@@ -31,6 +31,7 @@ import android.net.LocalServerSocket;
 import android.os.Build;
 import android.os.HandlerThread;
 import android.os.Looper;
+import android.os.SystemProperties;
 import android.preference.PreferenceManager;
 import android.provider.Settings;
 import android.provider.Settings.SettingNotFoundException;
@@ -57,6 +58,7 @@ import com.android.telephony.Rlog;
 
 import java.io.FileDescriptor;
 import java.io.PrintWriter;
+import java.lang.reflect.Constructor;
 import java.util.HashMap;
 import java.util.Map;
 
@@ -104,6 +106,88 @@ public class PhoneFactory {
 
     //***** Class Methods
 
+    /**
+     * vivo 1907N IMS bring-up: real root cause of IMS/VoLTE never registering, traced all the
+     * way down from MtkQualifiedNetworksService's own constructor (mediatek-telephony-
+     * common.jar, byte-identical to stock): it checks `mPhone.mCi instanceof MtkRIL` and
+     * silently no-ops its whole periodic qualified-networks evaluation loop otherwise, logging
+     * just "Constructor: mPhone.mCi is not instanceof MTKRIL!" - a message nothing downstream of
+     * that class ever surfaces prominently. MtkTelephonyComponentFactory (also already in that
+     * jar) was clearly meant to inject this via overriding a makeRil() factory method, but that
+     * indirection doesn't exist in this AOSP version at all - this class constructs RIL
+     * directly, no factory call in between. MtkRIL extends RIL with an identical constructor
+     * signature (Context, int, int, Integer) to the real one, so this is a direct swap, not a
+     * port - except mediatek-telephony-common.jar is a dex_import prebuilt, which Soong
+     * explicitly forbids as a compile-time dependency of a real java_library (it "can only be
+     * installed", per build/soong/java/java.go's own comment on DexImport) - so this can't be a
+     * plain "new MtkRIL(...)" the compiler resolves normally. Falls back to real RIL if the
+     * class genuinely isn't present (e.g. building for a different, non-MTK device), so this is
+     * never a hard dependency.
+     */
+    private static CommandsInterface newRil(Context context, int preferredNetworkType,
+            int cdmaSubscription, Integer instanceId) {
+        try {
+            Class<?> mtkRilClass = Class.forName("com.mediatek.internal.telephony.MtkRIL");
+            Constructor<?> ctor = mtkRilClass.getConstructor(
+                    Context.class, int.class, int.class, Integer.class);
+            CommandsInterface ci = (CommandsInterface) ctor.newInstance(
+                    context, preferredNetworkType, cdmaSubscription, instanceId);
+            // vivo 1907N IMS bring-up: logcat proved completely unreliable for observing this
+            // call site (neither this class's own pre-existing Rlog.i calls nor a newly added
+            // Rlog.w ever showed up in *any* boot capture, across multiple reboots and even
+            // with 8MB logd buffers - reason never pinned down, even though other classes'
+            // Rlog output, e.g. RIL.java's RILJ tag, is captured fine and continuously). A
+            // system property is immune to logd buffering/dropping, so use that instead as the
+            // one source of truth for which class actually got wired up as mCi. MUST use the
+            // "ril." prefix (radio_prop in property_contexts) - "debug." (debug_prop) is NOT
+            // set_prop-granted to the radio domain in this sepolicy tree, which crash-looped
+            // com.android.phone on every boot (uncaught RuntimeException from
+            // SystemProperties.set on an avc denial) until this was caught. Also wrapped in its
+            // own try/catch now so a permission problem can never crash phone bring-up again,
+            // whatever the property name ends up being.
+            setRilDebugPropSafely("ril.mtkril.instance" + instanceId, ci.getClass().getName());
+            return ci;
+        } catch (ReflectiveOperationException e) {
+            // First round of testing this got as far as MtkRIL's own constructor actually
+            // throwing (ReflectiveOperationException here was InvocationTargetException, not a
+            // ClassNotFoundException/NoSuchMethodException), so surface the real wrapped cause
+            // - that's the thing that actually needs fixing next, not this reflection shim.
+            Throwable cause = e.getCause() != null ? e.getCause() : e;
+            String detail = cause.getClass().getSimpleName() + ":" + cause.getMessage();
+            setRilDebugPropSafely("ril.mtkril.instance" + instanceId,
+                    "FALLBACK:" + truncateForProp(detail));
+            // Try logging the full stack trace too, in case this boot's log capture behaves
+            // differently now that the crash-loop is gone - costs nothing if it's still dropped.
+            Rlog.w(LOG_TAG, "newRil FALLBACK for instanceId=" + instanceId, e);
+            return new RIL(context, preferredNetworkType, cdmaSubscription, instanceId);
+        } catch (LinkageError e) {
+            // vivo 1907N IMS bring-up: MtkTelephonyComponentFactory's own Class.forName() hit
+            // this for real (MtkServiceStateTracker overriding a "final" ServiceStateTracker
+            // method - not a ReflectiveOperationException, uncatchable by the block above since
+            // LinkageError extends Error). Hardening this call site the same way defensively,
+            // even though it hasn't actually hit this yet, since we now know the failure mode
+            // exists and this is just as uncaught otherwise.
+            Rlog.w(LOG_TAG, "newRil LINKERR for instanceId=" + instanceId, e);
+            setRilDebugPropSafely("ril.mtkril.instance" + instanceId,
+                    "LINKERR:" + truncateForProp(e.getClass().getSimpleName() + ":" + e.getMessage()));
+            return new RIL(context, preferredNetworkType, cdmaSubscription, instanceId);
+        }
+    }
+
+    private static String truncateForProp(String s) {
+        // PROP_VALUE_MAX is 91 bytes (bionic system_properties.h); leave room for "FALLBACK:".
+        final int max = 80;
+        return s.length() > max ? s.substring(0, max) : s;
+    }
+
+    private static void setRilDebugPropSafely(String key, String value) {
+        try {
+            SystemProperties.set(key, value);
+        } catch (RuntimeException e) {
+            Rlog.w(LOG_TAG, "setRilDebugPropSafely: couldn't set " + key + ": " + e);
+        }
+    }
+
     public static void makeDefaultPhones(Context context) {
         makeDefaultPhone(context);
     }
@@ -172,7 +256,7 @@ public class PhoneFactory {
                     networkModes[i] = RILConstants.PREFERRED_NETWORK_MODE;
 
                     Rlog.i(LOG_TAG, "Network Mode set to " + Integer.toString(networkModes[i]));
-                    sCommandsInterfaces[i] = new RIL(context,
+                    sCommandsInterfaces[i] = newRil(context,
                             RadioAccessFamily.getRafFromNetworkType(networkModes[i]),
                             cdmaSubscription, i);
                 }
@@ -299,7 +383,9 @@ public class PhoneFactory {
 
             int cdmaSubscription = CdmaSubscriptionSourceManager.getDefault(context);
             for (int i = prevActiveModemCount; i < activeModemCount; i++) {
-                sCommandsInterfaces[i] = new RIL(context, RadioAccessFamily.getRafFromNetworkType(
+                // vivo 1907N IMS bring-up: same MtkRIL swap as newRil()'s use in
+                // makeRadioInterfaces() above - see that method's own comment.
+                sCommandsInterfaces[i] = newRil(context, RadioAccessFamily.getRafFromNetworkType(
                         RILConstants.PREFERRED_NETWORK_MODE),
                         cdmaSubscription, i);
                 sPhones[i] = createPhone(context, i);
diff --git a/src/java/com/android/internal/telephony/RIL.java b/src/java/com/android/internal/telephony/RIL.java
index 70431f6a0c..0fd2c40cba 100644
--- a/src/java/com/android/internal/telephony/RIL.java
+++ b/src/java/com/android/internal/telephony/RIL.java
@@ -160,7 +160,8 @@ public class RIL extends BaseCommands implements CommandsInterface {
     public static final HalVersion RADIO_HAL_VERSION_2_0 = new HalVersion(2, 0);
 
     // IRadio version
-    private HalVersion mRadioVersion = RADIO_HAL_VERSION_UNKNOWN;
+    // vivo 1907N IMS bring-up: widened alongside mRilHandler/mPhoneId - MtkRIL reads this too.
+    protected HalVersion mRadioVersion = RADIO_HAL_VERSION_UNKNOWN;
 
     //***** Instance Variables
 
@@ -194,7 +195,12 @@ public class RIL extends BaseCommands implements CommandsInterface {
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     AtomicBoolean mTestingEmergencyCall = new AtomicBoolean(false);
 
-    final Integer mPhoneId;
+    // vivo 1907N IMS bring-up: was package-private. Same story as mRilHandler above - MtkRIL
+    // (different package) reads this constantly (17 call sites in its own jar, all reads, no
+    // writes - stays final). Confirmed via a live IllegalAccessError crash-looping
+    // com.android.phone (which in turn triggered RescueParty into requesting a recovery wipe -
+    // see MtkRIL.java:552 in the crash log). Widening only.
+    protected final Integer mPhoneId;
 
     private boolean mUseOldMncMccFormat;
 
@@ -219,7 +225,8 @@ public class RIL extends BaseCommands implements CommandsInterface {
     private final SparseArray<Set<Integer>> mDisabledRadioServices = new SparseArray<>();
 
     /* default work source which will blame phone process */
-    private WorkSource mRILDefaultWorkSource;
+    // vivo 1907N IMS bring-up: widened alongside mRilHandler/mPhoneId - MtkRIL reads this too.
+    protected WorkSource mRILDefaultWorkSource;
 
     /* Worksource containing all applications causing wakelock to be held */
     private WorkSource mActiveWakelockWorkSource;
@@ -229,10 +236,11 @@ public class RIL extends BaseCommands implements CommandsInterface {
     /** Radio bug detector instance */
     private RadioBugDetector mRadioBugDetector = null;
 
-    private boolean mIsCellularSupported;
+    // vivo 1907N IMS bring-up: widened alongside mRilHandler/mPhoneId - MtkRIL reads both.
+    protected boolean mIsCellularSupported;
     private RadioResponse mRadioResponse;
     private RadioIndication mRadioIndication;
-    private volatile IRadio mRadioProxy = null;
+    public volatile IRadio mRadioProxy = null;
     private DataResponse mDataResponse;
     private DataIndication mDataIndication;
     private MessagingResponse mMessagingResponse;
@@ -249,7 +257,13 @@ public class RIL extends BaseCommands implements CommandsInterface {
     private final SparseArray<BinderServiceDeathRecipient> mDeathRecipients = new SparseArray<>();
     private final SparseArray<AtomicLong> mServiceCookies = new SparseArray<>();
     private final RadioProxyDeathRecipient mRadioProxyDeathRecipient;
-    final RilHandler mRilHandler;
+    // vivo 1907N IMS bring-up: was "final" package-private. MtkRIL (com.mediatek.internal.
+    // telephony, a different package) extends RIL and its constructor replaces this with its
+    // own MtkRilHandler subclass - stock MTK's own patched RIL.java clearly widens this same
+    // way. Confirmed via a live IllegalAccessError otherwise: "Field '...RIL.mRilHandler' is
+    // inaccessible to class ...MtkRIL". Widening only, nothing narrowed - no other caller of
+    // this field (all within this same class/package already) loses any capability.
+    public RilHandler mRilHandler;
     private MockModem mMockModem;
 
     // Thread-safe HashMap to map from RIL_REQUEST_XXX constant to HalVersion.
@@ -1176,7 +1190,9 @@ public class RIL extends BaseCommands implements CommandsInterface {
         }
     }
 
-    private RILRequest obtainRequest(int request, Message result, WorkSource workSource) {
+    // vivo 1907N IMS bring-up: widened alongside the fields above - MtkRIL calls the 3-arg
+    // overload directly (invoke-virtual). The varargs overload left untouched, not referenced.
+    protected RILRequest obtainRequest(int request, Message result, WorkSource workSource) {
         RILRequest rr = RILRequest.obtain(request, result, workSource);
         addRequest(rr);
         return rr;
@@ -5113,7 +5129,30 @@ public class RIL extends BaseCommands implements CommandsInterface {
         }
     }
 
-    void processRequestAck(int serial) {
+    // vivo 1907N IMS bring-up: single-arg overload for MtkRadioExIndication (mediatek-telephony-
+    // common.jar, prebuilt) - its own proprietary "RadioEx" HIDL indication channel isn't one of
+    // the 7 standard service categories above (RADIO/DATA/MESSAGING/MODEM/NETWORK/SIM/VOICE), so
+    // there's no correct "service" value to route an ack through sendAck() for it. Deliberately
+    // NOT guessing at one - an ack sent to the wrong HAL service proxy risks silently desyncing
+    // wakelock/proxy state, which is worse than just not acking a channel this framework has no
+    // real model of. Confirmed via a live NoSuchMethodError crash-looping com.android.phone
+    // (MtkRadioExIndication.java:713 etc., called from many RadioEx indication handlers).
+    // MUST be public, not protected: unlike MtkRIL, MtkRadioExIndication does NOT extend RIL -
+    // it's an unrelated class (different package) holding an "mRil" reference and calling
+    // through it, so subclass-only protected access doesn't apply (confirmed via a second live
+    // crash, IllegalAccessError this time, after protected wasn't enough).
+    public void processIndication(int indicationType) {
+        if (indicationType == RadioIndicationType.UNSOLICITED_ACK_EXP) {
+            riljLoge("processIndication(int): UNSOLICITED_ACK_EXP on the RadioEx channel has no"
+                    + " service to ack through - not sending one");
+        }
+    }
+
+    // vivo 1907N IMS bring-up: MtkRadioExResponse/MtkRadioExResponseV2 (mediatek-telephony-
+    // common.jar, prebuilt) call this compositionally through an "mRil"-typed field, not by
+    // inheritance (neither extends RIL) - same category as processIndication/riljLog, needs
+    // public, not just package-private/protected.
+    public void processRequestAck(int serial) {
         RILRequest rr;
         synchronized (mRequestList) {
             rr = mRequestList.get(serial);
@@ -5605,8 +5644,11 @@ public class RIL extends BaseCommands implements CommandsInterface {
         return rc;
     }
 
+    // vivo 1907N IMS bring-up: MtkRIL calls this directly by class-qualified static reference
+    // (Lcom/android/internal/telephony/RIL;->retToString), not the subclass-qualified pattern -
+    // widened to public for the same reason as unsljLog* above.
     @UnsupportedAppUsage
-    static String retToString(int req, Object ret) {
+    public static String retToString(int req, Object ret) {
         if (ret == null) return "";
         switch (req) {
             // Don't log these return values, for privacy's sake.
@@ -5732,8 +5774,10 @@ public class RIL extends BaseCommands implements CommandsInterface {
      *
      * @param rilVer is the version of the ril or -1 if disconnected.
      */
+    // vivo 1907N IMS bring-up: MtkRadioIndication (mediatek-telephony-common.jar, prebuilt)
+    // calls this compositionally through an "mRil"-typed field - needs public.
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    void notifyRegistrantsRilConnectionChanged(int rilVer) {
+    public void notifyRegistrantsRilConnectionChanged(int rilVer) {
         mRilVersion = rilVer;
         if (mRilConnectedRegistrants != null) {
             mRilConnectedRegistrants.notifyRegistrants(
@@ -5791,12 +5835,14 @@ public class RIL extends BaseCommands implements CommandsInterface {
         }
     }
 
+    // vivo 1907N IMS bring-up: riljLog/riljLoge widened alongside the fields above - MtkRIL
+    // calls both directly (invoke-virtual). riljLogv left untouched, not referenced by it.
     @UnsupportedAppUsage
-    void riljLog(String msg) {
+    public void riljLog(String msg) {
         Rlog.d(RILJ_LOG_TAG, msg + (" [PHONE" + mPhoneId + "]"));
     }
 
-    void riljLoge(String msg) {
+    public void riljLoge(String msg) {
         Rlog.e(RILJ_LOG_TAG, msg + (" [PHONE" + mPhoneId + "]"));
     }
 
@@ -5804,6 +5850,24 @@ public class RIL extends BaseCommands implements CommandsInterface {
         Rlog.v(RILJ_LOG_TAG, msg + (" [PHONE" + mPhoneId + "]"));
     }
 
+    // vivo 1907N IMS bring-up: these three used to be static methods directly on RIL itself in
+    // whatever older AOSP snapshot MtkRIL (mediatek-telephony-common.jar, prebuilt) was compiled
+    // against - upstream later moved them out to RILUtils (same package, still public static)
+    // without leaving anything behind here. MtkRIL's own bytecode still calls
+    // RIL.convertNullToEmptyString()/primitiveArrayToArrayList()/requestToString() directly, so
+    // provide them back as thin static delegates rather than touching RILUtils's real callers.
+    protected static String convertNullToEmptyString(String string) {
+        return RILUtils.convertNullToEmptyString(string);
+    }
+
+    protected static ArrayList<Byte> primitiveArrayToArrayList(byte[] arr) {
+        return RILUtils.primitiveArrayToArrayList(arr);
+    }
+
+    protected static String requestToString(int request) {
+        return RILUtils.requestToString(request);
+    }
+
     boolean isLogOrTrace() {
         return RIL.RILJ_LOGD || Trace.isTagEnabled(Trace.TRACE_TAG_NETWORK);
     }
@@ -5812,8 +5876,11 @@ public class RIL extends BaseCommands implements CommandsInterface {
         return RIL.RILJ_LOGV || Trace.isTagEnabled(Trace.TRACE_TAG_NETWORK);
     }
 
+    // vivo 1907N IMS bring-up: unsljLog/unsljLogMore/unsljLogRet/unsljLogvRet below and
+    // retToString above are all called compositionally by MtkRadioIndication/MtkRadioExIndication
+    // (mediatek-telephony-common.jar, prebuilt) through an "mRil"-typed field - need public.
     @UnsupportedAppUsage
-    void unsljLog(int response) {
+    public void unsljLog(int response) {
         String logStr = RILUtils.responseToString(response);
         if (RIL.RILJ_LOGD) {
             riljLog("[UNSL]< " + logStr);
@@ -5822,7 +5889,7 @@ public class RIL extends BaseCommands implements CommandsInterface {
     }
 
     @UnsupportedAppUsage
-    void unsljLogMore(int response, String more) {
+    public void unsljLogMore(int response, String more) {
         String logStr = RILUtils.responseToString(response) + " " + more;
         if (RIL.RILJ_LOGD) {
             riljLog("[UNSL]< " + logStr);
@@ -5831,7 +5898,7 @@ public class RIL extends BaseCommands implements CommandsInterface {
     }
 
     @UnsupportedAppUsage
-    void unsljLogRet(int response, Object ret) {
+    public void unsljLogRet(int response, Object ret) {
         String logStr = RILUtils.responseToString(response) + " " + retToString(response, ret);
         if (RIL.RILJ_LOGD) {
             riljLog("[UNSL]< " + logStr);
@@ -5840,7 +5907,7 @@ public class RIL extends BaseCommands implements CommandsInterface {
     }
 
     @UnsupportedAppUsage
-    void unsljLogvRet(int response, Object ret) {
+    public void unsljLogvRet(int response, Object ret) {
         String logStr = RILUtils.responseToString(response) + " " + retToString(response, ret);
         if (RIL.RILJ_LOGV) {
             riljLogv("[UNSL]< " + logStr);
diff --git a/src/java/com/android/internal/telephony/RadioIndication.java b/src/java/com/android/internal/telephony/RadioIndication.java
index d947d53611..4179869ea7 100644
--- a/src/java/com/android/internal/telephony/RadioIndication.java
+++ b/src/java/com/android/internal/telephony/RadioIndication.java
@@ -147,6 +147,16 @@ public class RadioIndication extends IRadioIndication.Stub {
         mRil.setRadioState(state, false /* forceNotifyRegistrants */);
     }
 
+    // vivo 1907N IMS bring-up: MtkRadioIndication (mediatek-telephony-common.jar, prebuilt)
+    // overrides radioStateChanged() and calls this directly (invoke-virtual, self-qualified -
+    // true inherited access, not composition) - this used to be a method directly on
+    // RadioIndication in whatever AOSP snapshot it was built against; upstream inlined it into
+    // radioStateChanged() as RILUtils.convertHalRadioState() without leaving a trace. Kept
+    // protected since only confirmed used via subclass inheritance so far.
+    protected int getRadioStateFromInt(int state) {
+        return RILUtils.convertHalRadioState(state);
+    }
+
     public void callStateChanged(int indicationType) {
         mRil.processIndication(RIL.RADIO_SERVICE, indicationType);
 
diff --git a/src/java/com/android/internal/telephony/ServiceStateTracker.java b/src/java/com/android/internal/telephony/ServiceStateTracker.java
index efc79a44c2..abbcb45bb3 100755
--- a/src/java/com/android/internal/telephony/ServiceStateTracker.java
+++ b/src/java/com/android/internal/telephony/ServiceStateTracker.java
@@ -3249,13 +3249,21 @@ public class ServiceStateTracker extends Handler {
         mRatLog.log(mSS.toString());
     }
 
+    // vivo 1907N IMS bring-up: dropped "final" - MtkServiceStateTracker (mediatek-telephony-
+    // common.jar, prebuilt) overrides both. Unlike every other fix in this patch set (all
+    // visibility), this one is a hard java.lang.LinkageError at class-load time, not a
+    // catchable exception - confirmed it crashes com.android.phone before even reaching
+    // MtkTelephonyComponentFactory's constructor, uncatchable by the newRil()-style
+    // try/catch(ReflectiveOperationException) since LinkageError extends Error. Also hardened
+    // makeDefaultInstance() below to catch Throwable, not just ReflectiveOperationException, so
+    // the next such issue (if any) fails safe instead of crashing phone bring-up again.
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    protected final void log(String s) {
+    protected void log(String s) {
         Rlog.d(LOG_TAG, "[" + mPhone.getPhoneId() + "] " + s);
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    protected final void loge(String s) {
+    protected void loge(String s) {
         Rlog.e(LOG_TAG, "[" + mPhone.getPhoneId() + "] " + s);
     }
 
diff --git a/src/java/com/android/internal/telephony/SmsStorageMonitor.java b/src/java/com/android/internal/telephony/SmsStorageMonitor.java
index b890a4da4d..3295556191 100755
--- a/src/java/com/android/internal/telephony/SmsStorageMonitor.java
+++ b/src/java/com/android/internal/telephony/SmsStorageMonitor.java
@@ -50,20 +50,25 @@ public class SmsStorageMonitor extends Handler {
     /** Radio is ON */
     private static final int EVENT_RADIO_ON = 3;
 
+    // vivo 1907N IMS bring-up: mContext/mWakeLock/mPhone/mStorageAvailable/mCi widened from
+    // private/package-private to protected - MtkSmsStorageMonitor (mediatek-telephony-
+    // common.jar, prebuilt) extends this from a different package and reads all of them via
+    // inheritance (mCi confirmed read-only there, kept final). Same pattern as MtkRIL/
+    // MtkSubscriptionController.
     /** Context from phone object passed to constructor. */
-    private final Context mContext;
+    protected final Context mContext;
 
     /** Wake lock to ensure device stays awake while dispatching the SMS intent. */
-    private PowerManager.WakeLock mWakeLock;
+    protected PowerManager.WakeLock mWakeLock;
 
     private boolean mReportMemoryStatusPending;
 
     /** it is use to put in to extra value for SIM_FULL_ACTION and SMS_REJECTED_ACTION */
-    Phone mPhone;
+    protected Phone mPhone;
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    final CommandsInterface mCi;                            // accessed from inner class
-    boolean mStorageAvailable = true;                       // accessed from inner class
+    protected final CommandsInterface mCi;                  // accessed from inner class
+    protected boolean mStorageAvailable = true;             // accessed from inner class
 
     /**
      * Hold the wake lock for 5 seconds, which should be enough time for
diff --git a/src/java/com/android/internal/telephony/SubscriptionController.java b/src/java/com/android/internal/telephony/SubscriptionController.java
index 82799bea8b..97c75b4b98 100644
--- a/src/java/com/android/internal/telephony/SubscriptionController.java
+++ b/src/java/com/android/internal/telephony/SubscriptionController.java
@@ -121,10 +121,21 @@ import java.util.stream.Collectors;
  * getSubInfoForSubscriber(INVALID_SUBSCRIPTION_ID) will return null.
  *
  */
+// vivo 1907N IMS bring-up: 15 fields/methods below widened from private to protected. Since
+// TelephonyComponentFactory injection now actually returns MtkTelephonyComponentFactory (this
+// class's own patch, PhoneFactory/TelephonyComponentFactory), its initSubscriptionController()
+// constructs MtkSubscriptionController (mediatek-telephony-common.jar, prebuilt) - a different-
+// package subclass reading/calling all of these via inheritance. Confirmed via a live
+// IllegalAccessError crash-looping com.android.phone (mDefaultPhoneId first, RescueParty
+// engaged safely via persist.sys.disable_rescue this time); the rest audited proactively via
+// the same self-qualified-smali-reference diff used for MtkRIL, to fix them all in one pass.
+// mType/setUiccState referenced by MtkSubscriptionController but not found anywhere in this
+// class under that name - likely another AOSP relocation/rename, not yet identified; not
+// touched by the constructor path that crashed, so deferred until it actually surfaces.
 public class SubscriptionController extends ISub.Stub {
     private static final String LOG_TAG = "SubscriptionController";
     private static final boolean DBG = false;
-    private static final boolean VDBG = Rlog.isLoggable(LOG_TAG, Log.VERBOSE);
+    protected static final boolean VDBG = Rlog.isLoggable(LOG_TAG, Log.VERBOSE);
     private static final boolean DBG_CACHE = false;
     private static final int DEPRECATED_SETTING = -1;
     private static final ParcelUuid INVALID_GROUP_UUID =
@@ -135,14 +146,14 @@ public class SubscriptionController extends ISub.Stub {
     private static final int SUB_ID_NOT_IN_SLOT = -2;
 
     // Lock that both mCacheActiveSubInfoList and mCacheOpportunisticSubInfoList use.
-    private Object mSubInfoListLock = new Object();
+    protected Object mSubInfoListLock = new Object();
 
     /* The Cache of Active SubInfoRecord(s) list of currently in use SubInfoRecord(s) */
-    private final List<SubscriptionInfo> mCacheActiveSubInfoList = new ArrayList<>();
+    protected final List<SubscriptionInfo> mCacheActiveSubInfoList = new ArrayList<>();
 
     /* Similar to mCacheActiveSubInfoList but only caching opportunistic subscriptions. */
     private List<SubscriptionInfo> mCacheOpportunisticSubInfoList = new ArrayList<>();
-    private AtomicBoolean mOpptSubInfoListChangedDirtyBit = new AtomicBoolean();
+    protected AtomicBoolean mOpptSubInfoListChangedDirtyBit = new AtomicBoolean();
 
     private static final Comparator<SubscriptionInfo> SUBSCRIPTION_INFO_COMPARATOR =
             (arg0, arg1) -> {
@@ -285,7 +296,7 @@ public class SubscriptionController extends ISub.Stub {
         }
     }
 
-    private static WatchedSlotIndexToSubIds sSlotIndexToSubIds = new WatchedSlotIndexToSubIds();
+    protected static WatchedSlotIndexToSubIds sSlotIndexToSubIds = new WatchedSlotIndexToSubIds();
 
     protected static WatchedInt sDefaultFallbackSubId =
             new WatchedInt(SubscriptionManager.INVALID_SUBSCRIPTION_ID) {
@@ -298,7 +309,7 @@ public class SubscriptionController extends ISub.Stub {
     };
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private static int mDefaultPhoneId = SubscriptionManager.DEFAULT_PHONE_INDEX;
+    protected static int mDefaultPhoneId = SubscriptionManager.DEFAULT_PHONE_INDEX;
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     private int[] colorArr;
@@ -306,7 +317,7 @@ public class SubscriptionController extends ISub.Stub {
     private RegistrantList mUiccAppsEnableChangeRegList = new RegistrantList();
 
     // The properties that should be shared and synced across grouped subscriptions.
-    private static final Set<String> GROUP_SHARING_PROPERTIES = new HashSet<>(Arrays.asList(
+    protected static final Set<String> GROUP_SHARING_PROPERTIES = new HashSet<>(Arrays.asList(
             SubscriptionManager.ENHANCED_4G_MODE_ENABLED,
             SubscriptionManager.VT_IMS_ENABLED,
             SubscriptionManager.WFC_IMS_ENABLED,
@@ -650,7 +661,7 @@ public class SubscriptionController extends ISub.Stub {
         return info;
     }
 
-    private String getOptionalStringFromCursor(Cursor cursor, String column, String defaultVal) {
+    protected String getOptionalStringFromCursor(Cursor cursor, String column, String defaultVal) {
         // Return defaultVal if the column doesn't exist.
         int columnIndex = cursor.getColumnIndex(column);
         return (columnIndex == -1) ? defaultVal : cursor.getString(columnIndex);
@@ -1581,7 +1592,7 @@ public class SubscriptionController extends ISub.Stub {
         return 0;
     }
 
-    private void updateDefaultSubIdsIfNeeded(int newDefault, int subscriptionType) {
+    protected void updateDefaultSubIdsIfNeeded(int newDefault, int subscriptionType) {
         if (DBG) {
             logdl("[updateDefaultSubIdsIfNeeded] newDefault=" + newDefault
                     + ", subscriptionType=" + subscriptionType);
@@ -2336,7 +2347,7 @@ public class SubscriptionController extends ISub.Stub {
     }
 
     // TODO: replace all updates with this helper method.
-    private int updateDatabase(ContentValues value, int subId, boolean updateEntireGroup) {
+    protected int updateDatabase(ContentValues value, int subId, boolean updateEntireGroup) {
         List<SubscriptionInfo> infoList = getSubscriptionsInGroup(getGroupUuid(subId),
                 mContext.getOpPackageName(), mContext.getAttributionTag());
         if (!updateEntireGroup || infoList == null || infoList.size() == 0) {
@@ -2957,7 +2968,7 @@ public class SubscriptionController extends ISub.Stub {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private void broadcastDefaultDataSubIdChanged(int subId) {
+    protected void broadcastDefaultDataSubIdChanged(int subId) {
         // Broadcast an Intent for default data sub change
         if (DBG) logdl("[broadcastDefaultDataSubIdChanged] subId=" + subId);
         Intent intent = new Intent(TelephonyIntents.ACTION_DEFAULT_DATA_SUBSCRIPTION_CHANGED);
@@ -3077,7 +3088,7 @@ public class SubscriptionController extends ISub.Stub {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private void validateSubId(int subId) {
+    protected void validateSubId(int subId) {
         if (DBG) logd("validateSubId subId: " + subId);
         if (!SubscriptionManager.isValidSubscriptionId(subId)) {
             throw new IllegalArgumentException("Invalid sub id passed as parameter");
@@ -4468,7 +4479,7 @@ public class SubscriptionController extends ISub.Stub {
         return true;
     }
 
-    private boolean isSubscriptionForRemoteSim(int subscriptionType) {
+    protected boolean isSubscriptionForRemoteSim(int subscriptionType) {
         return subscriptionType == SubscriptionManager.SUBSCRIPTION_TYPE_REMOTE_SIM;
     }
 
@@ -4491,7 +4502,7 @@ public class SubscriptionController extends ISub.Stub {
         mDefaultPhoneId = SubscriptionManager.DEFAULT_PHONE_INDEX;
     }
 
-    private void notifyOpportunisticSubscriptionInfoChanged() {
+    protected void notifyOpportunisticSubscriptionInfoChanged() {
         TelephonyRegistryManager trm =
                 (TelephonyRegistryManager)
                         mContext.getSystemService(Context.TELEPHONY_REGISTRY_SERVICE);
@@ -4778,7 +4789,7 @@ public class SubscriptionController extends ISub.Stub {
     /**
      * @hide
      */
-    private void setGlobalSetting(String name, int value) {
+    protected void setGlobalSetting(String name, int value) {
         Settings.Global.putInt(mContext.getContentResolver(), name, value);
         if (name == Settings.Global.MULTI_SIM_DATA_CALL_SUBSCRIPTION) {
             invalidateDefaultDataSubIdCaches();
diff --git a/src/java/com/android/internal/telephony/TelephonyComponentFactory.java b/src/java/com/android/internal/telephony/TelephonyComponentFactory.java
index dcbd2d5a95..345c7f70d2 100644
--- a/src/java/com/android/internal/telephony/TelephonyComponentFactory.java
+++ b/src/java/com/android/internal/telephony/TelephonyComponentFactory.java
@@ -60,6 +60,7 @@ import org.xmlpull.v1.XmlPullParserException;
 
 import java.io.File;
 import java.io.IOException;
+import java.lang.reflect.Constructor;
 import java.util.Arrays;
 import java.util.HashSet;
 import java.util.Set;
@@ -231,11 +232,70 @@ public class TelephonyComponentFactory {
 
     public static TelephonyComponentFactory getInstance() {
         if (sInstance == null) {
-            sInstance = new TelephonyComponentFactory();
+            sInstance = makeDefaultInstance();
         }
         return sInstance;
     }
 
+    /**
+     * vivo 1907N IMS bring-up: real device intent is telephony_injection.xml (parsed by
+     * injectTheComponentFactory() below) pointing PhoneGlobals at MtkTelephonyComponentFactory -
+     * confirmed genuinely empty (package="" jar="") in stock's own TeleService.apk too, so this
+     * AOSP-sanctioned mechanism is vestigial here; whatever real, patched PhoneFactory.java
+     * MediaTek/vivo actually ship must wire this up some other way we don't have source for.
+     * MtkTelephonyComponentFactory (mediatek-telephony-common.jar, prebuilt dex_import - can't
+     * be a compile-time dependency, same reason MtkRIL needed reflection in PhoneFactory)
+     * overrides ~50 of this class's factory methods (makePhone, makeImsPhoneCallTracker,
+     * makeGsmCdmaCallTracker, makeDcTracker, makeServiceStateTracker, makeUiccController, ...),
+     * i.e. this single substitution is what actually wires the *rest* of the MTK telephony
+     * stack in, not just the RIL swap. Falls back to the plain factory if the class or its
+     * no-arg constructor isn't there, so this is never a hard dependency.
+     */
+    private static TelephonyComponentFactory makeDefaultInstance() {
+        try {
+            Class<?> mtkFactoryClass =
+                    Class.forName("com.mediatek.internal.telephony.MtkTelephonyComponentFactory");
+            Constructor<?> ctor = mtkFactoryClass.getConstructor();
+            TelephonyComponentFactory instance = (TelephonyComponentFactory) ctor.newInstance();
+            setDiagPropSafely("ril.mtktcf.result", instance.getClass().getName());
+            return instance;
+        } catch (ReflectiveOperationException e) {
+            Rlog.w(TAG, "MtkTelephonyComponentFactory not available, falling back to plain "
+                    + "TelephonyComponentFactory: " + e);
+            Throwable cause = e.getCause() != null ? e.getCause() : e;
+            String detail = cause.getClass().getSimpleName() + ":" + cause.getMessage();
+            setDiagPropSafely("ril.mtktcf.result",
+                    "FALLBACK:" + (detail.length() > 80 ? detail.substring(0, 80) : detail));
+            return new TelephonyComponentFactory();
+        } catch (LinkageError e) {
+            // vivo 1907N IMS bring-up: Class.forName() itself can throw this (not just
+            // ReflectiveOperationException) if any class MtkTelephonyComponentFactory's own
+            // method signatures reference - e.g. MtkServiceStateTracker overriding a "final"
+            // method on our side - fails ART's class verifier. Confirmed live: crashed
+            // com.android.phone before this method's try block could even catch it, since
+            // LinkageError extends Error, not Exception. Catching it explicitly here so any
+            // *other* such mismatch we haven't found yet fails safe instead of taking down
+            // phone bring-up again.
+            Rlog.w(TAG, "MtkTelephonyComponentFactory failed to link/verify, falling back to "
+                    + "plain TelephonyComponentFactory: " + e);
+            String detail = e.getClass().getSimpleName() + ":" + e.getMessage();
+            setDiagPropSafely("ril.mtktcf.result",
+                    "LINKERR:" + (detail.length() > 80 ? detail.substring(0, 80) : detail));
+            return new TelephonyComponentFactory();
+        }
+    }
+
+    // Same "ril." prefix trick as PhoneFactory.newRil() - radio_prop is already
+    // set_prop(radio, radio_prop)-granted, "debug."/persist.* is not, and this must never be
+    // able to crash phone bring-up regardless of sepolicy.
+    private static void setDiagPropSafely(String key, String value) {
+        try {
+            android.os.SystemProperties.set(key, value);
+        } catch (RuntimeException e) {
+            Rlog.w(TAG, "setDiagPropSafely: couldn't set " + key + ": " + e);
+        }
+    }
+
     /**
      * Inject TelephonyComponentFactory using a xml config file.
      * @param parser a nullable {@link XmlResourceParser} created with the injection config file.
diff --git a/src/java/com/android/internal/telephony/TelephonyDevController.java b/src/java/com/android/internal/telephony/TelephonyDevController.java
index f97386df23..c8533e7cca 100644
--- a/src/java/com/android/internal/telephony/TelephonyDevController.java
+++ b/src/java/com/android/internal/telephony/TelephonyDevController.java
@@ -102,8 +102,16 @@ public class TelephonyDevController extends Handler {
     /**
      * each RIL call this interface to register/unregister the unsolicited hardware
      * configuration callback data it can provide.
+     *
+     * vivo 1907N IMS bring-up: was "static void". MtkRIL's own constructor (mediatek-
+     * telephony-common.jar, prebuilt) calls TelephonyDevController.getInstance().registerRIL(this)
+     * and its compiled bytecode expects an invoke-virtual, i.e. an instance method - confirmed via
+     * a live IncompatibleClassChangeError otherwise ("expected to be of type virtual but instead
+     * was found to be of type static"). Every field this touches is already static, and the only
+     * real call site (RIL.java, right below) already calls it through a "tdc" instance variable,
+     * so dropping "static" is a pure bytecode-shape change with the exact same source semantics.
      */
-    public static void registerRIL(CommandsInterface cmdsIf) {
+    public void registerRIL(CommandsInterface cmdsIf) {
         /* get the current configuration from this ril... */
         cmdsIf.getHardwareConfig(sRilHardwareConfig);
         /* ... process it ... */
@@ -117,7 +125,9 @@ public class TelephonyDevController extends Handler {
         cmdsIf.registerForHardwareConfigChanged(sTelephonyDevController, EVENT_HARDWARE_CONFIG_CHANGED, null);
     }
 
-    public static void unregisterRIL(CommandsInterface cmdsIf) {
+    // Kept symmetric with registerRIL above, same reasoning - not currently exercised by
+    // MtkRIL's own bytecode, but RIL.java's dispose() path may need the same shape.
+    public void unregisterRIL(CommandsInterface cmdsIf) {
         cmdsIf.unregisterForHardwareConfigChanged(sTelephonyDevController);
     }
 
diff --git a/src/java/com/android/internal/telephony/imsphone/ImsPhone.java b/src/java/com/android/internal/telephony/imsphone/ImsPhone.java
index 71e85f35b1..0520a1f880 100644
--- a/src/java/com/android/internal/telephony/imsphone/ImsPhone.java
+++ b/src/java/com/android/internal/telephony/imsphone/ImsPhone.java
@@ -133,6 +133,12 @@ import java.util.function.Consumer;
 /**
  * {@hide}
  */
+// vivo 1907N IMS bring-up: 16 fields/methods below widened from private/package-private to
+// protected - MtkImsPhone (mediatek-telephony-common.jar, prebuilt), now actually constructed
+// since MtkTelephonyComponentFactory.makeImsPhone() is wired in, reads/calls all of them via
+// inheritance. Found via the same self-qualified-smali-reference diff used for MtkRIL/
+// MtkSubscriptionController, applied proactively (not one crash at a time) since this class was
+// clearly next in the makePhone -> createImsPhone -> makeImsPhoneCallTracker chain.
 public class ImsPhone extends ImsPhoneBase {
     private static final String LOG_TAG = "ImsPhone";
     private static final boolean DBG = true;
@@ -248,14 +254,14 @@ public class ImsPhone extends ImsPhoneBase {
     }
 
     // Instance Variables
-    Phone mDefaultPhone;
+    protected Phone mDefaultPhone;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    ImsPhoneCallTracker mCT;
+    protected ImsPhoneCallTracker mCT;
     ImsExternalCallTracker mExternalCallTracker;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private ArrayList <ImsPhoneMmiCode> mPendingMMIs = new ArrayList<ImsPhoneMmiCode>();
+    protected ArrayList <ImsPhoneMmiCode> mPendingMMIs = new ArrayList<ImsPhoneMmiCode>();
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private ServiceState mSS = new ServiceState();
+    protected ServiceState mSS = new ServiceState();
 
     private final ImsManagerFactory mImsManagerFactory;
 
@@ -279,7 +285,7 @@ public class ImsPhone extends ImsPhoneBase {
     private ImsRegistrationCallbackHelper mImsMmTelRegistrationHelper;
 
     // The roaming state if currently in service, or the last roaming state when was in service.
-    private boolean mLastKnownRoamingState = false;
+    protected boolean mLastKnownRoamingState = false;
 
     private boolean mIsInImsEcm = false;
 
@@ -1080,7 +1086,7 @@ public class ImsPhone extends ImsPhoneBase {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private boolean isValidCommandInterfaceCFAction (int commandInterfaceCFAction) {
+    protected boolean isValidCommandInterfaceCFAction (int commandInterfaceCFAction) {
         switch (commandInterfaceCFAction) {
         case CF_ACTION_DISABLE:
         case CF_ACTION_ENABLE:
@@ -1093,7 +1099,7 @@ public class ImsPhone extends ImsPhoneBase {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private  boolean isCfEnable(int action) {
+    protected boolean isCfEnable(int action) {
         return (action == CF_ACTION_ENABLE) || (action == CF_ACTION_REGISTRATION);
     }
 
@@ -1129,7 +1135,7 @@ public class ImsPhone extends ImsPhoneBase {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private int getActionFromCFAction(int action) {
+    protected int getActionFromCFAction(int action) {
         switch(action) {
             case CF_ACTION_DISABLE: return ImsUtInterface.ACTION_DEACTIVATION;
             case CF_ACTION_ENABLE: return ImsUtInterface.ACTION_ACTIVATION;
@@ -1411,7 +1417,7 @@ public class ImsPhone extends ImsPhoneBase {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private void sendErrorResponse(Message onComplete) {
+    protected void sendErrorResponse(Message onComplete) {
         logd("sendErrorResponse");
         if (onComplete != null) {
             AsyncResult.forMessage(onComplete, null,
@@ -1482,7 +1488,7 @@ public class ImsPhone extends ImsPhoneBase {
         return ex;
     }
 
-    private void
+    protected void
     onNetworkInitiatedUssd(ImsPhoneMmiCode mmi) {
         logd("onNetworkInitiatedUssd");
         mMmiCompleteRegistrants.notifyRegistrants(
@@ -1728,7 +1734,7 @@ public class ImsPhone extends ImsPhoneBase {
         return cwInfos;
     }
 
-    private void
+    protected void
     sendResponse(Message onComplete, Object result, Throwable e) {
         if (onComplete != null) {
             CommandException ex = null;
@@ -1771,7 +1777,7 @@ public class ImsPhone extends ImsPhoneBase {
         return b;
     }
 
-    private void sendResponseOrRetryOnCsfbSs(SS ss, int what, Throwable e, Object obj) {
+    protected void sendResponseOrRetryOnCsfbSs(SS ss, int what, Throwable e, Object obj) {
         if (!isCsRetryException(e)) {
             sendResponse(ss.mOnComplete, obj, e);
             return;
@@ -2425,7 +2431,7 @@ public class ImsPhone extends ImsPhoneBase {
      * @return true if we are reporting not in service for CS domain over WWAN transport and WLAN
      * for PS domain over WWAN transport.
      */
-    private boolean isCsNotInServiceAndPsWwanReportingWlan(ServiceState ss) {
+    protected boolean isCsNotInServiceAndPsWwanReportingWlan(ServiceState ss) {
         // We can not get into this condition if we are in AP-Assisted mode.
         if (mDefaultPhone.getAccessNetworksManager() == null
                 || !mDefaultPhone.getAccessNetworksManager().isInLegacyMode()) {
@@ -2643,7 +2649,7 @@ public class ImsPhone extends ImsPhoneBase {
         pw.flush();
     }
 
-    private void logi(String s) {
+    protected void logi(String s) {
         Rlog.i(LOG_TAG, "[" + mPhoneId + "] " + s);
     }
 
@@ -2651,11 +2657,11 @@ public class ImsPhone extends ImsPhoneBase {
         Rlog.v(LOG_TAG, "[" + mPhoneId + "] " + s);
     }
 
-    private void logd(String s) {
+    protected void logd(String s) {
         Rlog.d(LOG_TAG, "[" + mPhoneId + "] " + s);
     }
 
-    private void loge(String s) {
+    protected void loge(String s) {
         Rlog.e(LOG_TAG, "[" + mPhoneId + "] " + s);
     }
 }
diff --git a/src/java/com/android/internal/telephony/imsphone/ImsPhoneCallTracker.java b/src/java/com/android/internal/telephony/imsphone/ImsPhoneCallTracker.java
index 48a863ee8e..2bbc8630c3 100644
--- a/src/java/com/android/internal/telephony/imsphone/ImsPhoneCallTracker.java
+++ b/src/java/com/android/internal/telephony/imsphone/ImsPhoneCallTracker.java
@@ -154,6 +154,12 @@ import java.util.regex.Pattern;
 /**
  * {@hide}
  */
+// vivo 1907N IMS bring-up: 25 fields/methods below widened from private to protected -
+// MtkImsPhoneCallTracker (mediatek-telephony-common.jar, prebuilt) reads/calls all of them via
+// inheritance. This is the actual class that reads persist.vendor.operator.optr and drives the
+// real IMS/VoLTE/VoWiFi registration sequence - everything this whole MtkTelephonyComponentFactory
+// injection effort was for. Found proactively via the same self-qualified-smali-reference diff
+// used for MtkRIL, not one crash at a time.
 public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     static final String LOG_TAG = "ImsPhoneCallTracker";
     static final String VERBOSE_STATE_TAG = "IPCTState";
@@ -193,7 +199,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     private MmTelFeature.MmTelCapabilities mMmTelCapabilities =
             new MmTelFeature.MmTelCapabilities();
 
-    private TelephonyMetrics mMetrics;
+    protected TelephonyMetrics mMetrics;
     private final Map<String, CallQualityMetrics> mCallQualityMetrics = new ConcurrentHashMap<>();
     private final ConcurrentLinkedQueue<CallQualityMetrics> mCallQualityMetricsHistory =
             new ConcurrentLinkedQueue<>();
@@ -406,7 +412,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
         }
     }
 
-    private final BroadcastReceiver mReceiver = new BroadcastReceiver() {
+    protected final BroadcastReceiver mReceiver = new BroadcastReceiver() {
         @Override
         public void onReceive(Context context, Intent intent) {
             if (intent.getAction().equals(CarrierConfigManager.ACTION_CARRIER_CONFIG_CHANGED)) {
@@ -524,7 +530,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
 
     //***** Instance Variables
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private ArrayList<ImsPhoneConnection> mConnections = new ArrayList<ImsPhoneConnection>();
+    protected ArrayList<ImsPhoneConnection> mConnections = new ArrayList<ImsPhoneConnection>();
     private RegistrantList mVoiceCallEndedRegistrants = new RegistrantList();
     private RegistrantList mVoiceCallStartedRegistrants = new RegistrantList();
 
@@ -571,7 +577,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     private final AtomicInteger mDefaultDialerUid = new AtomicInteger(NetworkStats.UID_ALL);
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private ImsPhoneConnection mPendingMO;
+    protected ImsPhoneConnection mPendingMO;
     private int mClirMode = CommandsInterface.CLIR_DEFAULT;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     private Object mSyncHold = new Object();
@@ -582,24 +588,24 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     private Message mPendingUssd = null;
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    ImsPhone mPhone;
+    protected ImsPhone mPhone;
 
     private boolean mDesiredMute = false;    // false = mute off
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private boolean mOnHoldToneStarted = false;
+    protected boolean mOnHoldToneStarted = false;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private int mOnHoldToneId = -1;
+    protected int mOnHoldToneId = -1;
 
-    private PhoneConstants.State mState = PhoneConstants.State.IDLE;
+    protected PhoneConstants.State mState = PhoneConstants.State.IDLE;
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private ImsManager mImsManager;
-    private ImsUtInterface mUtInterface;
+    protected ImsManager mImsManager;
+    protected ImsUtInterface mUtInterface;
 
-    private Call.SrvccState mSrvccState = Call.SrvccState.NONE;
+    protected Call.SrvccState mSrvccState = Call.SrvccState.NONE;
 
     private boolean mIsInEmergencyCall = false;
-    private boolean mIsDataEnabled = false;
+    protected boolean mIsDataEnabled = false;
 
     private int pendingCallClirMode;
     private int mPendingCallVideoState;
@@ -612,7 +618,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
     private boolean mAllowEmergencyVideoCalls = false;
     private boolean mIgnoreDataEnabledChangedForVideoCalls = false;
-    private boolean mIsViLteDataMetered = false;
+    protected boolean mIsViLteDataMetered = false;
     private boolean mAlwaysPlayRemoteHoldTone = false;
     private boolean mAutoRetryFailedWifiEmergencyCall = false;
     private boolean mSupportCepOnPeer = true;
@@ -620,7 +626,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     private boolean mSupportSdpForRtpHeaderExtensions = false;
     // Tracks the state of our background/foreground calls while a call hold/swap operation is
     // in progress. Values listed above.
-    private HoldSwapState mHoldSwitchingState = HoldSwapState.INACTIVE;
+    protected HoldSwapState mHoldSwitchingState = HoldSwapState.INACTIVE;
 
     private String mLastDialString = null;
     private ImsDialArgs mLastDialArgs = null;
@@ -648,13 +654,13 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
      * Carrier configuration option which determines whether adding a call during a video call
      * should be allowed.
      */
-    private boolean mAllowAddCallDuringVideoCall = true;
+    protected boolean mAllowAddCallDuringVideoCall = true;
 
     /**
      * Carrier configuration option which determines whether holding a video call
      * should be allowed.
      */
-    private boolean mAllowHoldingVideoCall = true;
+    protected boolean mAllowHoldingVideoCall = true;
 
     /**
      * Carrier configuration option which determines whether to notify the connection if a handover
@@ -957,7 +963,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
      * happens we need to prevent the IMS service config from being updated, as this will cause VT
      * to be disabled mid-call, resulting in an inability to un-pause the video.
      */
-    private boolean mShouldUpdateImsConfigOnDisconnect = false;
+    protected boolean mShouldUpdateImsConfigOnDisconnect = false;
 
     private Pair<Boolean, Integer> mPendingSilentRedialInfo = null;
 
@@ -991,7 +997,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
                 Executor executor);
     }
     private final ConnectorFactory mConnectorFactory;
-    private final FeatureConnector<ImsManager> mImsManagerConnector;
+    protected final FeatureConnector<ImsManager> mImsManagerConnector;
 
     // Used exclusively for IMS Registration related events for logging.
     private final LocalLog mRegLocalLog = new LocalLog(64);
@@ -2716,7 +2722,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private synchronized ImsPhoneConnection findConnection(final ImsCall imsCall) {
+    protected synchronized ImsPhoneConnection findConnection(final ImsCall imsCall) {
         for (ImsPhoneConnection conn : mConnections) {
             if (conn.getImsCall() == imsCall) {
                 return conn;
@@ -4873,7 +4879,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
     }
 
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.R, trackingBug = 170729553)
-    private void setVideoCallProvider(ImsPhoneConnection conn, ImsCall imsCall)
+    protected void setVideoCallProvider(ImsPhoneConnection conn, ImsCall imsCall)
             throws RemoteException {
         IImsVideoCallProvider imsVideoCallProvider =
                 imsCall.getCallSession().getVideoCallProvider();
@@ -4928,7 +4934,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
      * @param callSubject The call subject.
      * @return The call subject with invalid characters removed and escaping applied as required.
      */
-    private String cleanseInstantLetteringMessage(String callSubject) {
+    protected String cleanseInstantLetteringMessage(String callSubject) {
         if (TextUtils.isEmpty(callSubject)) {
             return callSubject;
         }
@@ -5004,7 +5010,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
         }
     }
 
-    private ImsException getImsManagerIsNullException() {
+    protected ImsException getImsManagerIsNullException() {
         return new ImsException("no ims manager", ImsReasonInfo.CODE_LOCAL_ILLEGAL_STATE);
     }
 
@@ -5149,7 +5155,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
         }
     }
 
-    private void maybeNotifyDataDisabled(boolean enabled, int reasonCode) {
+    protected void maybeNotifyDataDisabled(boolean enabled, int reasonCode) {
         if (!enabled) {
             // If data is disabled while there are ongoing VT calls which are not taking place over
             // wifi, then they should be disconnected to prevent the user from incurring further
@@ -5184,7 +5190,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
      *                            disabled.
      * @param reasonCode The {@link ImsReasonInfo} code for the data enabled state change.
      */
-    private void handleDataEnabledChange(boolean enabled, int reasonCode) {
+    protected void handleDataEnabledChange(boolean enabled, int reasonCode) {
         if (!enabled) {
             // If data is disabled while there are ongoing VT calls which are not taking place over
             // wifi, then they should be disconnected to prevent the user from incurring further
@@ -5250,7 +5256,7 @@ public class ImsPhoneCallTracker extends CallTracker implements ImsPullCall {
         }
     }
 
-    private void resetImsCapabilities() {
+    protected void resetImsCapabilities() {
         log("Resetting Capabilities...");
         boolean tmpIsVideoCallEnabled = isVideoCallEnabled();
         mMmTelCapabilities = new MmTelFeature.MmTelCapabilities();
