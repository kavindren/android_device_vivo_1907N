diff --git a/StubLibraries.bp b/StubLibraries.bp
index 32101c791242..60b529f8bf74 100644
--- a/StubLibraries.bp
+++ b/StubLibraries.bp
@@ -47,6 +47,11 @@ droidstubs {
         api_lint: {
             enabled: true,
             new_since: ":android.api.public.latest",
+            // vivo 1907N IMS bring-up: this variant had no baseline_file at all (unlike the
+            // system/test/module-lib variants below, which do), so widening ServiceState's
+            // members to protected for MtkServiceState had no grandfathering mechanism to fall
+            // into and failed the build outright. See core/api/lint-baseline.txt.
+            baseline_file: "core/api/lint-baseline.txt",
         },
     },
     dists: [
diff --git a/core/api/lint-baseline.txt b/core/api/lint-baseline.txt
new file mode 100644
index 000000000000..2951a2a4cc75
--- /dev/null
+++ b/core/api/lint-baseline.txt
@@ -0,0 +1,137 @@
+// Baseline format: 1.0
+InternalField: android.telephony.ServiceState#mCdmaDefaultRoamingIndicator:
+    Internal field mCdmaDefaultRoamingIndicator must not be exposed
+InternalField: android.telephony.ServiceState#mCdmaRoamingIndicator:
+    Internal field mCdmaRoamingIndicator must not be exposed
+InternalField: android.telephony.ServiceState#mCellBandwidths:
+    Internal field mCellBandwidths must not be exposed
+InternalField: android.telephony.ServiceState#mChannelNumber:
+    Internal field mChannelNumber must not be exposed
+InternalField: android.telephony.ServiceState#mCssIndicator:
+    Internal field mCssIndicator must not be exposed
+InternalField: android.telephony.ServiceState#mDataRegState:
+    Internal field mDataRegState must not be exposed
+InternalField: android.telephony.ServiceState#mIsEmergencyOnly:
+    Internal field mIsEmergencyOnly must not be exposed
+InternalField: android.telephony.ServiceState#mIsIwlanPreferred:
+    Internal field mIsIwlanPreferred must not be exposed
+InternalField: android.telephony.ServiceState#mIsManualNetworkSelection:
+    Internal field mIsManualNetworkSelection must not be exposed
+InternalField: android.telephony.ServiceState#mNetworkId:
+    Internal field mNetworkId must not be exposed
+InternalField: android.telephony.ServiceState#mNetworkRegistrationInfos:
+    Internal field mNetworkRegistrationInfos must not be exposed
+InternalField: android.telephony.ServiceState#mNrFrequencyRange:
+    Internal field mNrFrequencyRange must not be exposed
+InternalField: android.telephony.ServiceState#mOperatorAlphaLong:
+    Internal field mOperatorAlphaLong must not be exposed
+InternalField: android.telephony.ServiceState#mOperatorAlphaLongRaw:
+    Internal field mOperatorAlphaLongRaw must not be exposed
+InternalField: android.telephony.ServiceState#mOperatorAlphaShort:
+    Internal field mOperatorAlphaShort must not be exposed
+InternalField: android.telephony.ServiceState#mOperatorAlphaShortRaw:
+    Internal field mOperatorAlphaShortRaw must not be exposed
+InternalField: android.telephony.ServiceState#mOperatorNumeric:
+    Internal field mOperatorNumeric must not be exposed
+InternalField: android.telephony.ServiceState#mSystemId:
+    Internal field mSystemId must not be exposed
+InternalField: android.telephony.ServiceState#mVoiceRegState:
+    Internal field mVoiceRegState must not be exposed
+
+
+MissingNullability: android.telephony.ServiceState#mCellBandwidths:
+    Missing nullability on field `mCellBandwidths` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mNetworkRegistrationInfos:
+    Missing nullability on field `mNetworkRegistrationInfos` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mOperatorAlphaLong:
+    Missing nullability on field `mOperatorAlphaLong` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mOperatorAlphaLongRaw:
+    Missing nullability on field `mOperatorAlphaLongRaw` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mOperatorAlphaShort:
+    Missing nullability on field `mOperatorAlphaShort` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mOperatorAlphaShortRaw:
+    Missing nullability on field `mOperatorAlphaShortRaw` in class `class android.telephony.ServiceState`
+MissingNullability: android.telephony.ServiceState#mOperatorNumeric:
+    Missing nullability on field `mOperatorNumeric` in class `class android.telephony.ServiceState`
+
+
+MutableBareField: android.telephony.ServiceState#mCdmaDefaultRoamingIndicator:
+    Bare field mCdmaDefaultRoamingIndicator must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mCdmaRoamingIndicator:
+    Bare field mCdmaRoamingIndicator must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mCellBandwidths:
+    Bare field mCellBandwidths must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mChannelNumber:
+    Bare field mChannelNumber must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mCssIndicator:
+    Bare field mCssIndicator must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mDataRegState:
+    Bare field mDataRegState must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mIsEmergencyOnly:
+    Bare field mIsEmergencyOnly must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mIsIwlanPreferred:
+    Bare field mIsIwlanPreferred must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mIsManualNetworkSelection:
+    Bare field mIsManualNetworkSelection must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mNetworkId:
+    Bare field mNetworkId must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mNrFrequencyRange:
+    Bare field mNrFrequencyRange must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mOperatorAlphaLong:
+    Bare field mOperatorAlphaLong must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mOperatorAlphaLongRaw:
+    Bare field mOperatorAlphaLongRaw must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mOperatorAlphaShort:
+    Bare field mOperatorAlphaShort must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mOperatorAlphaShortRaw:
+    Bare field mOperatorAlphaShortRaw must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mOperatorNumeric:
+    Bare field mOperatorNumeric must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mSystemId:
+    Bare field mSystemId must be marked final, or moved behind accessors if mutable
+MutableBareField: android.telephony.ServiceState#mVoiceRegState:
+    Bare field mVoiceRegState must be marked final, or moved behind accessors if mutable
+
+
+ProtectedMember: android.telephony.ServiceState#equalsHandlesNulls(Object, Object):
+    Protected methods not allowed; must be public: method android.telephony.ServiceState.equalsHandlesNulls(Object,Object)}
+ProtectedMember: android.telephony.ServiceState#mCdmaDefaultRoamingIndicator:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mCdmaDefaultRoamingIndicator}
+ProtectedMember: android.telephony.ServiceState#mCdmaRoamingIndicator:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mCdmaRoamingIndicator}
+ProtectedMember: android.telephony.ServiceState#mCellBandwidths:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mCellBandwidths}
+ProtectedMember: android.telephony.ServiceState#mChannelNumber:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mChannelNumber}
+ProtectedMember: android.telephony.ServiceState#mCssIndicator:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mCssIndicator}
+ProtectedMember: android.telephony.ServiceState#mDataRegState:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mDataRegState}
+ProtectedMember: android.telephony.ServiceState#mIsEmergencyOnly:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mIsEmergencyOnly}
+ProtectedMember: android.telephony.ServiceState#mIsIwlanPreferred:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mIsIwlanPreferred}
+ProtectedMember: android.telephony.ServiceState#mIsManualNetworkSelection:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mIsManualNetworkSelection}
+ProtectedMember: android.telephony.ServiceState#mNetworkId:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mNetworkId}
+ProtectedMember: android.telephony.ServiceState#mNetworkRegistrationInfos:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mNetworkRegistrationInfos}
+ProtectedMember: android.telephony.ServiceState#mNrFrequencyRange:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mNrFrequencyRange}
+ProtectedMember: android.telephony.ServiceState#mOperatorAlphaLong:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mOperatorAlphaLong}
+ProtectedMember: android.telephony.ServiceState#mOperatorAlphaLongRaw:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mOperatorAlphaLongRaw}
+ProtectedMember: android.telephony.ServiceState#mOperatorAlphaShort:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mOperatorAlphaShort}
+ProtectedMember: android.telephony.ServiceState#mOperatorAlphaShortRaw:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mOperatorAlphaShortRaw}
+ProtectedMember: android.telephony.ServiceState#mOperatorNumeric:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mOperatorNumeric}
+ProtectedMember: android.telephony.ServiceState#mSystemId:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mSystemId}
+ProtectedMember: android.telephony.ServiceState#mVoiceRegState:
+    Protected fields not allowed; must be public: field android.telephony.ServiceState.mVoiceRegState}
+ProtectedMember: android.telephony.ServiceState#setFromNotifierBundle(android.os.Bundle):
+    Protected methods not allowed; must be public: method android.telephony.ServiceState.setFromNotifierBundle(android.os.Bundle)}
diff --git a/telephony/java/android/telephony/ServiceState.java b/telephony/java/android/telephony/ServiceState.java
index 6fe9bf97c5c3..e6bee8a71577 100644
--- a/telephony/java/android/telephony/ServiceState.java
+++ b/telephony/java/android/telephony/ServiceState.java
@@ -61,6 +61,19 @@ import java.util.stream.Collectors;
  * For historical reasons this class is not declared as final; however,
  * it should be treated as though it were final.
  */
+// vivo 1907N IMS bring-up: mediatek.telephony.MtkServiceState (mediatek-telephony-base.jar,
+// prebuilt - a different jar from mediatek-telephony-common.jar, so a different repo:
+// frameworks/base here, not frameworks/opt/telephony) extends this class directly and accesses
+// 21 fields/methods that were private in our current AOSP source. Live IllegalAccessError on
+// mNetworkRegistrationInfos led to the same full self-qualified-smali-reference audit used for
+// MtkServiceStateTracker: extracted every Lmediatek/telephony/MtkServiceState;-> self-reference,
+// matched against this file's declarations, widened every private one to protected in one pass.
+// mNetworkRegistrationInfos itself stays final - confirmed via smali (14 iget, 0 iput) that
+// MtkServiceState only ever reads it (mutates the List's contents, never reassigns the field),
+// so widening visibility alone is sufficient and correct - no relocation needed here, unlike
+// several fields in ServiceStateTracker/mTransportManager. Everything else accessed but not
+// found declared here is MtkServiceState's own member (its own cellular/RIL-transport-specific
+// accessor layer, e.g. getCellularDataRegState()) - no change needed for those.
 public class ServiceState implements Parcelable {
 
     static final String LOG_TAG = "PHONE";
@@ -275,8 +288,8 @@ public class ServiceState implements Parcelable {
                     | (1 << (RIL_RADIO_TECHNOLOGY_EVDO_B - 1))
                     | (1 << (RIL_RADIO_TECHNOLOGY_EHRPD - 1));
 
-    private int mVoiceRegState = STATE_OUT_OF_SERVICE;
-    private int mDataRegState = STATE_OUT_OF_SERVICE;
+    protected int mVoiceRegState = STATE_OUT_OF_SERVICE;
+    protected int mDataRegState = STATE_OUT_OF_SERVICE;
 
     /** @hide */
     @Retention(RetentionPolicy.SOURCE)
@@ -326,33 +339,27 @@ public class ServiceState implements Parcelable {
     private static final String EXTRA_SERVICE_STATE = "android.intent.extra.SERVICE_STATE";
 
 
-    private String mOperatorAlphaLong;
-    private String mOperatorAlphaShort;
-    private String mOperatorNumeric;
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P)
-    private boolean mIsManualNetworkSelection;
+    protected String mOperatorAlphaLong;
+    protected String mOperatorAlphaShort;
+    protected String mOperatorNumeric;
+    protected boolean mIsManualNetworkSelection;
 
-    private boolean mIsEmergencyOnly;
+    protected boolean mIsEmergencyOnly;
 
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
-    private boolean mCssIndicator;
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P)
-    private int mNetworkId;
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P)
-    private int mSystemId;
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
-    private int mCdmaRoamingIndicator;
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
-    private int mCdmaDefaultRoamingIndicator;
+    protected boolean mCssIndicator;
+    protected int mNetworkId;
+    protected int mSystemId;
+    protected int mCdmaRoamingIndicator;
+    protected int mCdmaDefaultRoamingIndicator;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
     private int mCdmaEriIconIndex;
     @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
     private int mCdmaEriIconMode;
 
     @FrequencyRange
-    private int mNrFrequencyRange;
-    private int mChannelNumber;
-    private int[] mCellBandwidths = new int[0];
+    protected int mNrFrequencyRange;
+    protected int mChannelNumber;
+    protected int[] mCellBandwidths = new int[0];
 
     /**
      *  ARFCN stands for Absolute Radio Frequency Channel Number. This field is current used for
@@ -360,12 +367,12 @@ public class ServiceState implements Parcelable {
      *  where it's for NR ARFCN (Reference: 3GPP TS 36.108) */
     private int mArfcnRsrpBoost = 0;
 
-    private final List<NetworkRegistrationInfo> mNetworkRegistrationInfos = new ArrayList<>();
+    protected final List<NetworkRegistrationInfo> mNetworkRegistrationInfos = new ArrayList<>();
 
-    private String mOperatorAlphaLongRaw;
-    private String mOperatorAlphaShortRaw;
+    protected String mOperatorAlphaLongRaw;
+    protected String mOperatorAlphaShortRaw;
     private boolean mIsDataRoamingFromRegistration;
-    private boolean mIsIwlanPreferred;
+    protected boolean mIsIwlanPreferred;
 
     /**
      * get String description of roaming type
@@ -1432,19 +1439,31 @@ public class ServiceState implements Parcelable {
      * @param b second obj
      * @return true if two objects equal or both are null
      */
-    @UnsupportedAppUsage(maxTargetSdk = Build.VERSION_CODES.P, trackingBug = 115609023)
-    private static boolean equalsHandlesNulls (Object a, Object b) {
+    protected static boolean equalsHandlesNulls (@Nullable Object a, @Nullable Object b) {
         return (a == null) ? (b == null) : a.equals (b);
     }
 
+    // vivo 1907N IMS bring-up: this method used to carry an internal-visibility javadoc marker
+    // (metalava's hide tag) - removed it (metalava's tag detection matched on the literal marker
+    // text even when it only appeared in prose describing the removal, hence this being a plain
+    // comment rather than inside the javadoc block below). Fixing this DID get the method
+    // correctly recorded in the API signature / hiddenapi flags, confirmed via
+    // api-stubs-docs-non-updatable_api.txt and hiddenapi-flags.csv - but that turned out NOT to
+    // be what was causing MtkServiceState's NoClassDefFoundError; the crash persisted unchanged
+    // after this fix compiled in (confirmed via dexdump of the actual on-device framework.jar).
+    // Read art/runtime/hidden_api.{h,cc} directly afterward: mediatek-telephony-base.jar and
+    // framework.jar are both under /system/framework, so DetermineDomainFromLocation() assigns
+    // them the same hiddenapi Domain (kPlatform), and CanAlwaysAccess() short-circuits same-
+    // domain callers before any per-member flag is even consulted - so hidden-API enforcement
+    // was never actually the mechanism at play here. Kept this fix anyway since it's a genuine,
+    // correct improvement (the method really is protected/inherited API now), just not the
+    // answer to the crash. The real cause is still unresolved as of this comment.
     /**
      * Set ServiceState based on intent notifier map.
      *
      * @param m intent notifier map
-     * @hide
      */
-    @UnsupportedAppUsage
-    private void setFromNotifierBundle(Bundle m) {
+    protected void setFromNotifierBundle(@NonNull Bundle m) {
         ServiceState ssFromBundle = m.getParcelable(EXTRA_SERVICE_STATE);
         if (ssFromBundle != null) {
             copyFrom(ssFromBundle);
