diff --git a/src/crypto/cipher_extra/e_tls.c b/src/crypto/cipher_extra/e_tls.c
index c462a2b3..f840a50e 100644
--- a/src/crypto/cipher_extra/e_tls.c
+++ b/src/crypto/cipher_extra/e_tls.c
@@ -596,3 +596,8 @@ const EVP_AEAD *EVP_aead_des_ede3_cbc_sha1_tls(void) {
 const EVP_AEAD *EVP_aead_des_ede3_cbc_sha1_tls_implicit_iv(void) {
   return &aead_des_ede3_cbc_sha1_tls_implicit_iv;
 }
+
+// vivo 1907N: mtk_agpsd (closed vendor blob) still imports this long-removed TLS null-cipher AEAD.
+// It only needs the symbol to resolve; returning NULL makes any attempt to use it fail cleanly
+// (EVP_AEAD_CTX_init rejects a NULL AEAD) instead of the process dying at load time.
+OPENSSL_EXPORT const EVP_AEAD *EVP_aead_null_sha1_tls(void) { return NULL; }
diff --git a/src/crypto/hpke/hpke.c b/src/crypto/hpke/hpke.c
index ff8b17b6..4aa7714a 100644
--- a/src/crypto/hpke/hpke.c
+++ b/src/crypto/hpke/hpke.c
@@ -794,6 +794,19 @@ const EVP_HPKE_AEAD *EVP_HPKE_CTX_aead(const EVP_HPKE_CTX *ctx) {
   return ctx->aead;
 }
 
+// vivo 1907N bring-up (LOS20/Android 13): plain compat alias for the pre-rename name of the
+// function above. Same lh_* rename story as crypto/lhash/lhash.c - /vendor/bin/mtk_agpsd
+// (closed blob) links against EVP_HPKE_get_aead directly: "CANNOT LINK EXECUTABLE
+// /vendor/bin/mtk_agpsd: cannot locate symbol EVP_HPKE_get_aead", found only after the lhash
+// fix let the linker get this far into mtk_agpsd's symbol table. Checked every other
+// crypto-family symbol this binary references against current BoringSSL - the rest are all
+// still exported, just declared in crypto/internal.h or fipsmodule/tls/internal.h rather than a
+// public header (OPENSSL_EXPORT applies regardless of which header declares it), or via the
+// DECLARE_ASN1_FUNCTIONS macro (d2i_X509_NAME/i2d_X509_NAME) - this was the only other rename.
+OPENSSL_EXPORT const EVP_HPKE_AEAD *EVP_HPKE_get_aead(const EVP_HPKE_CTX *ctx) {
+  return EVP_HPKE_CTX_aead(ctx);
+}
+
 const EVP_HPKE_KDF *EVP_HPKE_CTX_kdf(const EVP_HPKE_CTX *ctx) {
   return ctx->kdf;
 }
diff --git a/src/crypto/lhash/lhash.c b/src/crypto/lhash/lhash.c
index fbab430a..4a5068e3 100644
--- a/src/crypto/lhash/lhash.c
+++ b/src/crypto/lhash/lhash.c
@@ -348,3 +348,54 @@ void OPENSSL_lh_doall_arg(_LHASH *lh, void (*func)(void *, void *), void *arg) {
   // resizing is done here.
   lh_maybe_resize(lh);
 }
+
+// vivo 1907N bring-up (LOS20/Android 13): plain, untyped aliases for the classic OpenSSL-style
+// lhash API. Some point after LOS19.1's (Android 12) libcrypto.so snapshot, upstream BoringSSL
+// renamed these to the OPENSSL_lh_* names above and stopped exporting the bare lh_* symbols -
+// the public header (openssl/lhash.h) now explicitly says "lhash is an internal library and not
+// exported for use outside BoringSSL". Fine for anything built against a current NDK/AOSP
+// header, but vendor.mediatek's prebuilt /vendor/bin/mtk_agpsd (no source available to us -
+// closed blob) links directly against the old bare names: "CANNOT LINK EXECUTABLE
+// /vendor/bin/mtk_agpsd: cannot locate symbol lh_free" - crash-looping agpsd, so no AGPS
+// assistance data and a much slower/less reliable GPS fix. Same fix philosophy as
+// system/core/libutils/RefBase.cpp's incStrongRequireStrong() compat shim: restore the old
+// exported symbol as a trivial forwarder in the real AOSP source rather than touch the closed
+// vendor binary (impossible) or hack the vendor sysfs/linker config.
+OPENSSL_EXPORT _LHASH *lh_new(lhash_hash_func hash, lhash_cmp_func comp) {
+  return OPENSSL_lh_new(hash, comp);
+}
+
+OPENSSL_EXPORT void lh_free(_LHASH *lh) { OPENSSL_lh_free(lh); }
+
+OPENSSL_EXPORT size_t lh_num_items(const _LHASH *lh) {
+  return OPENSSL_lh_num_items(lh);
+}
+
+OPENSSL_EXPORT void *lh_retrieve(const _LHASH *lh, const void *data,
+                                  lhash_hash_func_helper call_hash_func,
+                                  lhash_cmp_func_helper call_cmp_func) {
+  return OPENSSL_lh_retrieve(lh, data, call_hash_func, call_cmp_func);
+}
+
+OPENSSL_EXPORT void *lh_retrieve_key(
+    const _LHASH *lh, const void *key, uint32_t key_hash,
+    int (*cmp_key)(const void *key, const void *value)) {
+  return OPENSSL_lh_retrieve_key(lh, key, key_hash, cmp_key);
+}
+
+OPENSSL_EXPORT int lh_insert(_LHASH *lh, void **old_data, void *data,
+                              lhash_hash_func_helper call_hash_func,
+                              lhash_cmp_func_helper call_cmp_func) {
+  return OPENSSL_lh_insert(lh, old_data, data, call_hash_func, call_cmp_func);
+}
+
+OPENSSL_EXPORT void *lh_delete(_LHASH *lh, const void *data,
+                                lhash_hash_func_helper call_hash_func,
+                                lhash_cmp_func_helper call_cmp_func) {
+  return OPENSSL_lh_delete(lh, data, call_hash_func, call_cmp_func);
+}
+
+OPENSSL_EXPORT void lh_doall_arg(_LHASH *lh, void (*func)(void *, void *),
+                                  void *arg) {
+  OPENSSL_lh_doall_arg(lh, func, arg);
+}
diff --git a/src/crypto/stack/stack.c b/src/crypto/stack/stack.c
index 97fae1b9..0f1974ab 100644
--- a/src/crypto/stack/stack.c
+++ b/src/crypto/stack/stack.c
@@ -388,6 +388,19 @@ err:
   return NULL;
 }
 
+// vivo 1907N bring-up: plain compat alias for the pre-rename name of the function above. Same
+// lh_*/OPENSSL_lh_* rename story as this repo's other compat shims (EVP_HPKE_get_aead in
+// crypto/hpke/hpke.c, lh_* family in crypto/lhash/lhash.c) - upstream BoringSSL renamed the
+// classic OpenSSL-style stack API to the OPENSSL_sk_* names above and stopped exporting the
+// bare sk_* symbols. The vivo stock /vendor/bin/hw/wpa_supplicant blob (no source available -
+// closed) links directly against the old bare name: "CANNOT LINK EXECUTABLE
+// /vendor/bin/hw/wpa_supplicant: cannot locate symbol sk_dup" - crash-looping supplicant, so no
+// Wi-Fi station mode at all. Same fix philosophy: restore the old exported symbol as a trivial
+// forwarder in the real AOSP source rather than touch the closed vendor binary (impossible).
+OPENSSL_EXPORT OPENSSL_STACK *sk_dup(const OPENSSL_STACK *sk) {
+  return OPENSSL_sk_dup(sk);
+}
+
 static size_t parent_idx(size_t idx) {
   assert(idx > 0);
   return (idx - 1) / 2;
@@ -532,3 +545,27 @@ void sk_pop_free_ex(OPENSSL_STACK *sk, OPENSSL_sk_call_free_func call_free_func,
                     OPENSSL_sk_free_func free_func) {
   OPENSSL_sk_pop_free_ex(sk, call_free_func, free_func);
 }
+
+// vivo 1907N: same bare-name compat aliases, next batch found by diffing /vendor/bin/mtk_agpsd's
+// undefined symbols against libcrypto.so's exports ("cannot locate symbol sk_deep_copy" etc.).
+OPENSSL_EXPORT OPENSSL_STACK *sk_deep_copy(
+    const OPENSSL_STACK *sk, OPENSSL_sk_call_copy_func call_copy_func,
+    OPENSSL_sk_copy_func copy_func, OPENSSL_sk_call_free_func call_free_func,
+    OPENSSL_sk_free_func free_func) {
+  return OPENSSL_sk_deep_copy(sk, call_copy_func, copy_func, call_free_func,
+                              free_func);
+}
+
+OPENSSL_EXPORT int sk_find(const OPENSSL_STACK *sk, size_t *out_index,
+                           const void *p,
+                           OPENSSL_sk_call_cmp_func call_cmp_func) {
+  return OPENSSL_sk_find(sk, out_index, p, call_cmp_func);
+}
+
+OPENSSL_EXPORT void *sk_set(OPENSSL_STACK *sk, size_t i, void *p) {
+  return OPENSSL_sk_set(sk, i, p);
+}
+
+OPENSSL_EXPORT void *sk_shift(OPENSSL_STACK *sk) {
+  return OPENSSL_sk_shift(sk);
+}
diff --git a/src/crypto/x509/x509_vfy.c b/src/crypto/x509/x509_vfy.c
index 9b013883..49488cc1 100644
--- a/src/crypto/x509/x509_vfy.c
+++ b/src/crypto/x509/x509_vfy.c
@@ -1630,6 +1630,12 @@ X509_STORE_CTX *X509_STORE_CTX_new(void) {
   return OPENSSL_zalloc(sizeof(X509_STORE_CTX));
 }
 
+// vivo 1907N: pre-removal export that /vendor/bin/mtk_agpsd (closed blob) still links against.
+// It always just zeroed the caller's struct.
+OPENSSL_EXPORT void X509_STORE_CTX_zero(X509_STORE_CTX *ctx) {
+  OPENSSL_memset(ctx, 0, sizeof(*ctx));
+}
+
 void X509_STORE_CTX_free(X509_STORE_CTX *ctx) {
   if (ctx == NULL) {
     return;
