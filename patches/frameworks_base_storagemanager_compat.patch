diff --git a/core/java/android/os/storage/StorageManager.java b/core/java/android/os/storage/StorageManager.java
index d9604b3f0145..fe6f806a44a6 100644
--- a/core/java/android/os/storage/StorageManager.java
+++ b/core/java/android/os/storage/StorageManager.java
@@ -1706,6 +1706,18 @@ public class StorageManager {
         return RoSystemProperties.CRYPTO_FILE_ENCRYPTED;
     }
 
+    /** {@hide}
+     * CryptKeeper (the pre-FBE full-disk-encryption "encrypting..." bounce screen) was removed
+     * from AOSP years ago, but this static method's absence is a hard NoSuchMethodError for any
+     * priv-app still compiled against an SDK where it existed (e.g. this device's stock MTK IMS
+     * blob, com.mediatek.ims - crashes MwiServiceHandlerThread/WifiPdnHandler otherwise).
+     * CryptKeeper no longer exists, so the device is never in a cryptkeeper bounce: always false.
+     */
+    @UnsupportedAppUsage
+    public static boolean inCryptKeeperBounce() {
+        return false;
+    }
+
     /** {@hide} */
     public static boolean isFileEncryptedEmulatedOnly() {
         return SystemProperties.getBoolean(StorageManager.PROP_EMULATE_FBE, false);
